name: Deploy Backend (Spring Boot)

on:
  push:
    branches: [master, main]
    paths:
      - 'sh-platform-auth/**'
      - 'sh-platform-common/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build with Gradle
        run: |
          chmod +x gradlew
          ./gradlew :sh-platform-auth:build -x test

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 140.245.95.162
          username: ubuntu
          password: ${{ secrets.OCI_SSH_PW }}
          script: |
            set -e

            sudo systemctl stop sh-platform 2>/dev/null || true

            cd /home/ubuntu/sh-platform
            git pull origin main

            ./gradlew :sh-platform-auth:clean :sh-platform-auth:build -x test -q

            sudo systemctl start sh-platform
            echo "Deploy complete!"
