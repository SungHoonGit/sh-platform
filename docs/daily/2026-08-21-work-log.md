# 2026-08-21 작업 일지

## 작업 환경
- 날짜: 2026-08-21 (금)
- 시간: ~18:00 기준
- 브랜치: master

## 오늘 완료된 작업
1. **설계문서 011 보강**: AI/RAG 확장 로드맵 §6 추가 (Gamma 등 PPT 트렌드 조사 반영, B안 권장 — LLM 슬라이드 JSON + 자체 렌더링)
2. **Phase 1 인프라 정리**: portfolio 모듈/옛 모놀리스 삭제, nginx/deploy/local-db-init/플랫폼 프레임 메뉴 정리. 서버 작업(systemd 유닛 제거, JAR 삭제, portfolio_platform DB 백업 후 DROP) 완료
3. **Phase 2 스켈레톤 교정**: 패키지 `com.resume.platform` → `com.shplatform.resume`, SecurityConfig를 scraper 패턴 JWT 필터체인으로 교체
4. **SSOT 인프라 도입**: `infra/services.yml` 단일 소스 + `scripts/render_config.py`(prometheus/promtail/systemd/nginx 생성, --check 드리프트 검증). deploy 워크플로우에 설정 동기화 스텝 추가. 서버 적용·검증 완료(systemd 유닛 diff SAME)
5. **Phase 3 (1차)**: DDL(`docs/resume/ddl-resume-v1.sql` — 항목 8테이블 + 공통 5테이블) + Profile(GET/upsert) + Career(CRUD) 백엔드 구현, 단위 테스트 11건 통과
6. **버그 수정**: 배포 워크플로우 git pull → fetch+reset(force-push 후 divergent 방지), OpenApiConfig 옛 패키지 선언 잔존 수정, resume DDL 자동 실행 추가
7. **문서**: 가이드 008(SSOT), 학습 005(IaC/GitOps 패턴)

## 변경된 파일 목록
- `infra/services.yml`, `scripts/render_config.py`, `infra/generated/*`: SSOT 신규
- `.github/workflows/deploy-backend.yml`: 설정 동기화 + resume DDL + fetch/reset
- `modules/resume/backend/src/**`: 패키지 이동, SecurityConfig, Profile/Career 도메인 17파일
- `docs/resume/ddl-resume-v1.sql`: 신규 DDL
- `docs/plans/011-...-design.md`: §6 AI 로드맵
- `docs/guides/008-...`, `docs/learnings/005-...`: 문서
- `AGENTS.md`: SSOT 규칙 추가

## 이슈 및 해결
- **prometheus localhost:8083 잔존 타겟**: portfolio 제거 후에도 타겟 남아 svc-down 알림 발생 직전 → 서버에서 수동 제거, 이후 SSOT으로 근본 해결
- **mysqldump "column statistics" 경고**: MariaDB 미지원 옵션 경고, 무해 (백업 정상 완료 확인)
- **배포 시 divergent branches 오류**: force-push(amend)로 서버와 이력 갈라짐 → 워크플로우를 fetch+hard reset으로 변경
- **curl 400 (Tomcat HTML)**: 터미널 붙여넣기 이스케이프(`^[[200~`)가 TOKEN에 섞임 → 토큰 재설정 필요
- **OpenApiConfig 미스캔**: 패키지 선언이 옛 이름이라 빈 미생성 → 수정
- **엔티티 protected 생성자**: domain에서 new 불가 → 정적 팩토리 `create(userId)` 도입

## 커밋 내역
| 시간 | 커밋 | 내용 |
|------|------|------|
| - | 2f7a310 | 설계문서 011 |
| - | 4b58d5d | 설계문서 011 §6 AI 로드맵 |
| - | b71d640 | Phase 1 인프라 정리 |
| - | fcc0617 | portfolio 삭제 누락 보완 |
| - | dda412a | Phase 2 스켈레톤 교정 |
| - | dc303e3 | SSOT 도입 (amend: bfafc0a 교체) |
| - | c7cacbd | 학습 문서 005 |
| - | a478998 | 배포 fetch+reset 수정 |
| - | ddb6480 | Phase 3 Profile/Career CRUD |
| - | 3befb6b | OpenApiConfig 패키지 수정 |
| - | 2bfd6dc | resume DDL 자동 실행 |

## 향후 작업
1. Phase 3 나머지 6개 도메인 (educations/skills/certificates/projects/introductions/portfolio-items) — `docs/daily/2026-08-21-todo.md` 참고
2. 배포 검증: Swagger에서 Profile/Career 노출 확인, curl 재테스트(깨끗한 토큰)
3. Phase 4: `/view` 조립 API + React SPA
