# 2026-09-11 작업 일지

## 작업 환경
- 날짜: 2026-09-11 (금)
- 브랜치: master

## 오늘 완료된 작업
1. **자격증 항목별 보임/숨김 — 이력서 단위 on/off** (사용자 제안·승인)
   - 배경: 자격증은 사용자 단위 공유 데이터(`resume_certificates.user_id`)라 신규 취득 시 모든 이력서에 다 나타남. 사용자 질문(별도 자격증 관리 페이지 vs 자격증 란 토글) → **항목별 on/off 토글** 확정 (별도 페이지 없음). 사용자 9/11 기사 합격 언급(자격증 등록 맥락)
   - 설계: 문서 `sectionConfig` JSON의 `certificates` 항목에 `"hiddenItemIds":[...]` 로 이력서별 숨김 자격증 저장. PDF는 서버측 필터, 웹 프리뷰는 클라이언트측 필터. 편집 화면(`/view`)은 전체 목록 유지
   - 백엔드:
     - `ResumePdfServiceImpl`: `DocumentOption`에 `Set<Long> hiddenCertificateIds` 추가, `parseHiddenCertificateIds()`(certificates 항목의 hiddenItemIds 파싱, 실패 시 빈 set) + `applyHiddenCertificates()`(이력서 자격증 필터 후 객체 재구성), generatePdf에서 적용
     - `ResumeDocumentServiceImpl.validateSectionConfig`: `hiddenItemIds`가 배열이 아니면 `INVALID_INPUT` 검증
   - 프론트:
     - `types/document.ts`: `SectionItem.hiddenItemIds?: number[]` 추가
     - `CrudSection.tsx`: `rowToggle` prop(보임/숨김 버튼) — inline/비-inline 양쪽 행 렌더링에 배치
     - `EditPage.tsx`: certificates 섹션에 rowToggle 배선(`toggleCertificateHidden` → `persistSections`에 hiddenItemIds 반영, 빈 배열이면 필드 생략)
     - `ResumeViewPage.tsx`: documentId가 있을 때 `/documents`의 sectionConfig에서 hiddenItemIds 파싱해 프리뷰 자격증 필터
   - 테스트: `ResumePdfBlankPageTest.hiddenCertificates_areNotRendered` — hiddenItemIds=[3]일 때 PDF에 "정보처리기사" 포함·"SQLD" 제외 검증 (헬퍼 `viewWithCertificates()` 추가). 전체 `:modules:resume:backend:test` BUILD SUCCESSFUL, `npm run build`(tsc+vite) 성공
   - 커밋 `dabe9be` → CI 배포 성공(5m48s)

2. **숨김 항목 PDF 표시 문제 원인 분석 + 2개 결함 수정** (사용자 보고: 미리보기엔 영역 안 보이는데 PDF엔 보임)
   - **재현 검증(서버 데이터 기준)**: 공유 링크(`2b8209d49b84418cae6d3428d1c8edcd`, 문서 6, SARAMIN) 공개 API 확인 — skills `included:false`(정상 숨김), 프로젝트 `included:true` 그리고 자격증 `hiddenItemIds` 없음. 실제 PDF 다운로드·텍스트 추출(pypdf)로 스킬 미출력 확인(섹션 숨김 → PDF 필터 정상 동작). 코드 대조: 미리보기(ResumeViewPage/ShareViewPage)와 PDF(서버 `parseSectionKeys`)가 **같은 문서의 같은 sectionConfig**를 읽고 동일하게 필터링 → 둘이 어긋나려면 '저장 유실'이 원인으로 특정
   - **결함 1 — 공유 페이지 미리보기에 hiddenItemIds 미적용**: 자격증 on/off는 내 이력서 미리보기(ResumeViewPage)·PDF(서버)만 반영되고 공유 프리뷰(ShareViewPage)는 숨긴 자격증이 그대로 보였음 → `ShareViewPage`에 `hiddenCertIds` 파싱 + `filteredView`를 템플릿에 전달 (커밋 `260bff5`)
   - **결함 2 — 저장 PUT 병렬 발사로 인한 갱신 유실(race)**: `persistSections`가 연속 토글(예: 프로젝트 숨김 후 다른 섹션 토글) 시 이전 PUT이 나중 요청의 최신 `included` 값을 덮어써 서버는 옛 값(config 포함=true)으로 남고, 로컬 미리보기만 숨겨져 PDF에 영역이 보이는 현상 유발 가능 → PUT을 `saveChain`으로 **직렬화** + 헤더에 "저장 중.../저장 실패 — 새로고침 필요" 인디케이터 추가 (커밋 `5a2ffe5`)
   - `npm run build` 성공, 커밋 2건 + docs 로그 커밋, CI 배포 2회 성공(단독 5m3s/5m3s×2, 병합 4m53s)

## 변경된 파일 목록
- `modules/resume/backend/.../domain/ResumePdfServiceImpl.java`: hiddenItemIds 파싱·자격증 필터링 (`DocumentOption.hiddenCertificateIds`, `parseHiddenCertificateIds`, `applyHiddenCertificates`)
- `modules/resume/backend/.../domain/ResumeDocumentServiceImpl.java`: `hiddenItemIds` 배열 형식 검증
- `modules/resume/backend/src/test/.../domain/ResumePdfBlankPageTest.java`: `hiddenCertificates_areNotRendered`·`viewWithCertificates()` 추가
- `modules/resume/frontend/src/types/document.ts`: `SectionItem.hiddenItemIds` 추가
- `modules/resume/frontend/src/components/CrudSection.tsx`: `rowToggle` prop (행별 보임/숨김 버튼)
- `modules/resume/frontend/src/pages/EditPage.tsx`: 자격증 행 토글 핸들러 + persistSections hiddenItemIds 반영
- `modules/resume/frontend/src/pages/ResumeViewPage.tsx`: 프리뷰 hiddenItemIds 필터
- `modules/resume/frontend/src/pages/ShareViewPage.tsx`: 공유 프리뷰 hiddenItemIds 필터 (`260bff5`)
- `modules/resume/frontend/src/pages/EditPage.tsx`: 저장 PUT 직렬화(saveChain) + 저장 상태 인디케이터 (`5a2ffe5`)
- `docs/daily/2026-09-11-work-log.md`: 신규 (본 일지)

## 이슈 및 해결
- **진행 중이던 PDF 제목 고아 방지(어제 2차 피드백) 사용자 확인**: 사용자 CD "일단 괜찮은듯" → 클래식/모던 수정(커밋 `ed469ab`) 배포 결과 OK
- **별도 자격증 관리 페이지 필요 여부**: 기존 자격증은 이미 사용자 단위 공통 데이터로 재사용 중 → 별도 페이지 없이 "이 이력서에서 보임/숨김"만 문서 단위 저장으로 해결. 신규 기능 추가/변경 비용 최소화(배포 없는 DB 설정 값으로 유지)

## 커밋 내역
| 시간 | 커밋 | 내용 |
|------|------|------|
| - | dabe9be | feat(resume): 자격증 항목별 보임/숨김 — 이력서 단위 on/off |
| - | cd50d1d | docs: 2026-09-11 작업 일지 — 자격증 항목별 보임/숨김 기록 |
| - | 260bff5 | fix(resume): 공유 페이지 미리보기에도 문서별 숨김 자격증(hiddenItemIds) 필터 적용 |
| - | 5a2ffe5 | fix(resume): 섹션 편성 저장 PUT 직렬화 — 미리보기/PDF 어긋남 방지 |

## 향후 작업
1. **숨김 저장 유실 재확인**: 배포 후 (프로젝트 숨김 → "저장 중..." 확인 → 미리보기 → PDF) 순서로 재현 여부 확인. 여전히 재현 시 DevTools Network의 PUT 응답 상태 확인 (사용자 확인 대기)
2. **자격증 토글 실사용 확인**: 편집 화면 자격증 행 "보임/숨김" → 프리뷰·PDF 반영 검증 (기사 합격 등록 후 활용)
3. **PDF 1차 수정 재검증**: ① 클래식 섹션 제목+내용 동일 페이지 ② 모던 폰트 축소/자기소개 한 영역
4. **실사용 검증 이어서**: 기술 스택 자동완성/유사검색, 지원 관리 스크랩 → 매칭 배지, 경력 상세/작업물 가져오기/썸네일
5. **확장 (백로그)**: 매칭 점수 산식(가중치), 공고 목록/스크랩 리스트 매칭 배지, AI 자소 첨삭, 이메일 알림