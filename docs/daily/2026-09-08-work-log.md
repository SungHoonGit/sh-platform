# 2026-09-08 작업 일지

## 작업 환경
- 날짜: 2026-09-08 (화)
- 브랜치: master (원격 `369a296` 상태에서 시작)

## 오늘 완료된 작업
1. **pull + 원격 동기화 확인**: `git pull origin master` — Already up to date (`369a296`)
2. **모든 데이터 섹션 순번(reorder) 관리 API 구현** (경력/스킬/자격증/학력/프로젝트/자기소개/포트폴리오 7종)
   - `ReorderRequest` DTO 신규: `record ReorderRequest(@NotEmpty List<@NotNull Long> ids)`
   - 7개 서비스 인터페이스 + 구현체에 `reorderXxx(...)` 추가 — 소유 id 조회 → 전달 순서대로 `displayOrder` 재지정 → `saveAll`, 비소유 id는 `ErrorCode.FORBIDDEN`
   - 7개 컨트롤러에 `PUT /api/v1/{section}/reorder` 엔드포인트 추가
   - JUnit 5 테스트 추가 완료(7개 ServiceImplTest): 성공/순서 역전/FORBIDDEN/빈 목록 케이스
   - 검증: `./gradlew :modules:resume:backend:test --tests "*ServiceImplTest*"` 47s BUILD SUCCESSFUL
3. **편집 화면 드래그&드롭 "아래로 이동" 버그 수정** (`EditPage.tsx`)
   - 원인: `reorderSections`가 드롭 대상 항상 앞(insert-before)에 삽입 → 아래로 드래그해도 바로 아래가 안 되던 문제
   - 수정: `onDragOver`에서 `e.clientY`와 대상 rect 중앙 비교로 before/after 판단, `reorderSections(draggedKey, targetKey, pos)`에서 인덱스 보정(insert-before→after), 경계 인디케이터는 위는 border-t / 아래는 border-b로 구분
4. **CrudSection 리오더 UI + 자기소개 인라인 항목 상시 표시**
   - `itemList`로 추출해 inline(자기소개)에서도 '추가' 버튼 전에 항목 목록(소제목)이 항상 보이게 수정 (기존 `editing !== null` 게이트 제거)
   - 각 항목 행에 ▲▼ 순서 버튼 + 드래그 핸들(⋮⋮) 추가
   - 드롭/▲▼ 시 `PUT ${endpoint}/reorder` 호출(`persistOrder`) 후 리스트 갱신, 위/아래 경계 인디케이터(border-t/bottom) 표시
   - 검증: `npm run build` (tsc -b + vite build) 성공
5. **작업 규칙 확립**: 미팅 후 사용자 규칙 — 작업 시작 시 `git pull origin master` → 오늘 작업 일지 확인 → 커밋/푸시 순서. AGENTS.md에 반영 예정.
6. **reorder 동작 실사용 이슈 수정 (CI 배포 후)** — "Failed to execute 'json' on 'Response': Unexpected end of JSON input"
   - 원인: reorder/delete가 `ResponseEntity.noContent()`(204, 빈 body) 반환, 프론트 `request()`(client.ts)가 무조건 `res.json()` 호출 → 파싱 오류 → `onChanged()` 미실행 → 순서 미반영
   - 수정: `request()`에서 `204` 또는 `Content-Length: 0`이면 `json()` 파싱 생략하고 `undefined` 반환
   - 검증: `npm run build` 통과
7. **자기소개 카드 파란 표시 버그 + 섹션 카드 자체 드래그 구현**
   - 자기소개(인라인) 항목 위/아래 파란 인디케이터가 안 보이던 원인: 카드 기본 `border-gray-100`이 모든 면 border-color를 지정해 같은 우선순위의 `border-blue-400`이 덮였음 → 오버 시엔 gray 색상을 제거하고 `border-blue-400` + `border-t/b-2`만 적용
   - 섹션 카드 자체 드래그: 각 카드 상단에 ⋮⋮ 핸들(드래그 그립) 추가, 카드 전체를 드롭 타깃으로 만들어 위/아래 절반으로 before/after 판단 → 구성 편성 패널과 동일한 순서 저장(`reorderSections`) 공유
   - CrudSection에 `sectionDragActive` prop 추가 — 카드 드래그 중엔 항목 행 드래그 이벤트를 무시해 이벤트 충돌 방지
   - 검증: `npm run build` 통과
8. **이력서 문서 목록 순번 관리** (사용자 요청 — 목록 페이지에서 각 이력서 카드 순번 지정)
   - DDL: `docs/resume/ddl-resume-v9.sql` — `resume_documents.display_order INT NOT NULL DEFAULT 0` (멱등 ADD + 생성순 백필), 배포 워크플로우에 v9 라인 추가
   - 백엔드: `ResumeDocumentEntity.displayOrder` + `updateDisplayOrder()`, 레포 `findByUserIdOrderByDisplayOrderAscIdAsc`, 서비스 `reorderDocuments()`(비소유 id FORBIDDEN) + 생성 시 max+1 순번 부여, 컨트롤러 `PUT /documents/reorder`, `DocumentResponse`에 displayOrder 추가
   - 테스트: `ResumeDocumentServiceImplTest` reorder 성공/FORBIDDEN/빈 목록 3건 추가 + 레포 메서드 변경·`DocumentResponse` 파라미터 추가로 인한 기존 테스트 갱신(PDF 6건, Share 1건, `ReflectionTestUtils`로 id 세팅)
   - 검증: `./gradlew :modules:resume:backend:test` 전체 BUILD SUCCESSFUL
   - 프론트: ResumesPage 카드에 ▲▼ + ⋮⋮ 드래그 순번 UI + 카드 드롭 타깃(파란 인디케이터) + `PUT /documents/reorder` 호출, `npm run build` 통과
   - 이슈: DocumentsController에 `ReorderRequest` import 누락 → 컴파일 에러 수정 (추가 1줄)
9. **문서 목록 드래그 UX 개선** (사용자 요청 — 핸들이 작아 불편 + 라이브 리오더 원하는 지적)
   - 드래그 영역 확대: 카드 최상단 ▲▼/⋮⋮ 바 전체를 드래그 핸들로 (`draggable`), 드래그 시 카드 전체를 드래그 이미지로(`setDragImage`)
   - 라이브 리오더: 드래그 중 `dragover`마다 현재 순서를 실시간 재배치(`reorderDocuments` 함수형 setState, 드래그된 카드가 목록을 밀며 이동) — 저장은 drop 시에만 `PUT /documents/reorder`
   - 취소 복원: dragStart 시 원본 순서 스냅샷(`origSnapshot` ref), drop 미처리 시 dragEnd에서 원상 복구
   - 대상 카드 표시는 outline으로 변경(레이아웃 흔들림 없음), 드래그 중인 카드는 opacity-40
   - 검증: `npm run build` 통과
10. **편집 페이지 섹션 카드 드래그를 동일 UX로 통일** (사용자 요청 — "편집의 영역들도 동일하게")
   - 피클 ⋮⋮ 핸들(pill) 제거 → CrudSection 헤더 바 전체를 드래그 핸들로 (`dragHandle` prop 추가, cursor-grab/select-none, hover 배경)
   - 카드 래퍼에 라이브 리오더(`reorderSectionsLive`, order 재할당으로 헤더·구성 패널 실시간 재배치), drop 시에만 `persistSections`(PUT)
   - 스냅샷/복원 로직(`origSnapshot`·`dropHandled` ref, `restoreOriginalOrder`)을 구성 편성(aside) 드래그와도 공유해 어디에 놓든 동일하게 동작
   - dropPos는 aside 인디케이터용으로 유지, 카드 over 표시는 outline으로 통일, hint 문구 갱신
   - 검증: `npm run build` 통과

## 변경된 파일 목록
- `modules/resume/backend/.../api/dto/ReorderRequest.java`: 신규 DTO
- `modules/resume/backend/.../domain/{Career,Certificate,Education,Skill,Project,Introduction,PortfolioItem}Service.java`: reorder 메서드 선언
- `modules/resume/backend/.../domain/{...}ServiceImpl.java`: reorder 구현
- `modules/resume/backend/.../api/{...}Controller.java`: `PUT /{section}/reorder` 엔드포인트
- `modules/resume/backend/src/main/java/com/shplatform/resume/api/DocumentsController.java`: `PUT /documents/reorder` + ReorderRequest import
- `modules/resume/backend/.../infrastructure/entity/ResumeDocumentEntity.java`: displayOrder 필드
- `modules/resume/backend/.../infrastructure/repository/ResumeDocumentRepository.java`: 정렬 메서드 변경
- `modules/resume/backend/.../domain/ResumeDocumentService(Impl).java`: reorderDocuments + 생성 순번
- `modules/resume/backend/.../api/dto/DocumentResponse.java`: displayOrder 추가
- `docs/resume/ddl-resume-v9.sql`: 신규 (display_order)
- `.github/workflows/deploy-backend.yml`: v9 DDL 라인 추가
- `modules/resume/backend/src/test/.../ResumeDocumentServiceImplTest.java` + PDF/Share 테스트: reorder 테스트·생성자 파라미터 갱신
- `modules/resume/frontend/src/pages/ResumesPage.tsx`: 문서 카드 ▲▼/⋮⋮ 드래그 순번 UI + 드롭 인디케이터
- `modules/resume/backend/src/test/.../domain/{...}ServiceImplTest.java`: reorder 테스트 추가(7개)
- `modules/resume/frontend/src/components/CrudSection.tsx`: inline 상시 표시 + ▲▼/드래그 리오더 UI + reorder API 호출 + 인라인 파란 인디케이터 수정 + `sectionDragActive` prop
- `modules/resume/frontend/src/pages/EditPage.tsx`: 드래그 before/after 판단 수정 + 카드 상단 ⋮⋮ 드래그 핸들(섹션 카드 이동) + 인디케이터
- `modules/resume/frontend/src/api/client.ts`: 204/빈 body 응답에서 `json()` 파싱 생략 (reorder·delete 오류 수정)
- `docs/daily/2026-09-08-work-log.md`: 신규 (본 일지)

## 이슈 및 해결
- (없음) — 로컬 테스트가 매번 Gradle 데몬 기동으로 느림 → 배포 서버(CI)로 검증하는 편이 빠름을 확인. 프론트 빌드/백엔드 테스트 모두 로컬 통과.

## 커밋 내역
| 시간 | 커밋 | 내용 |
|------|------|------|
| - | (진행) | feat: 데이터 섹션 순번 관리 — reorder API 7종 + 드래그/▲▼ UI + 인라인 목록 상시 표시 |

## 향후 작업
1. **GitHub Actions 배포 확인** — reorder API Swagger 반영 + 편집 화면에서 드래그/▲▼ 동작 실사용 확인
2. **공유 링크·이력서 PDF 실사용자 최종 확인** (이전 세션 이월)
3. **로드맵 백로그**: 공고-이력서 키워드 매칭 점수, AI 자소 첨삭, 이메일 알림