# 2026-08-28 작업 일지

## 작업 환경
- 날짜: 2026-08-28 (금)
- 시간: 세션 기준
- 브랜치: master

## 오늘 완료된 작업
1. **대시보드 '내 스크랩' 마감임박 카드** (platform/frontend)
   - `ScrapItemDto.deadline` 필드 추가 + deadlineBadge/daysUntilDeadline 유틸 추가 (scraper jobPlanet.ts 로직 복제)
   - 내 스크랩 목록을 마감임박(D-3 이내) 우선 정렬 + D-N 배지 표시 (D-0~3=amber, 마감=red)
   - 타입체크/oxlint 통과 → 커밋(push) → 배포
2. **Tailscale 개념 정리** (`docs/learnings/007-260828-tailscale-learning.md`)
   - WireGuard 기반 오버레이 네트워크, SSLVPN과의 차이, sh-platform 접근 맥락
3. **이력서 벤치마킹 Phase 3/4 마무리** (modules/resume/frontend)
   - FR-005: EditPage에 우측 고정(sticky) 목차 네비게이션 (클릭 → 섹션 scrollIntoView)
   - FR-008: 섹션 포함/숨김 토글 + 위/아래 순서 이동 → `PUT /documents/{id}`에 section_config 저장
     - 백엔드 section_config 컬럼·검증·API·뷰 렌더링은 이미 존재 → 프론트 UI만 추가
   - FR-004: 자기소개 섹션을 인라인 편집형으로 전환 (CrudSection에 `inline` 옵션, 목록 행 자체에서 편집)
   - Phase 4: 다중 이력서 정체성 문구 명시 ("항목 데이터는 모든 이력서 공유, 구성만 다름" + 구성 불러오기 라벨 명확화)
   - 프론트 빌드(tsc -b && vite build) 성공 → 커밋(push) → 배포
4. **뷰어 날짜/시간 구분 회귀 버그 수정** (modules/scraper/backend)
   - 원인: 블랙리스트 필터 분기가 `crawledAt`(날짜)/`runIds`(시간) 조건을 완전 무시하고
     해당 크롤러의 전체 공고를 로드 → 날짜/시간 구분 없이 "총 건이 다 보이는" 증상
   - 수정: 블랙 필터 분기에서도 날짜/run/사이트 조건 쿼리를 먼저 수행한 뒤 블랙만 메모리 필터
5. **차단 회사 관리 → 모달 팝업 전환** (modules/scraper/frontend)
   - 신규 공용 컴포넌트 `BlacklistManagerModal.tsx` (고정 오버레이 모달, 목록+해제)
   - Viewer.tsx / Search.tsx 의 인라인 펼침 패널을 모달로 교체 (추가가 많아져도 공간 비효율 없음)
   - 프론트 빌드(tsc -b && vite build) 성공
6. **뷰어 행의 차단(EyeOff) 버튼 위치 변경** (modules/scraper/frontend/Viewer.tsx)
   - 기존 테이블 마지막 컬럼 → ★(즐겨찾기/스크랩) 왼쪽(첫 컬럼)으로 이동 + 헤더 컬럼 순서 정렬
7. **이력서 공고탐색에 차단 기능 추가** (modules/resume/frontend)
   - `PostingsBrowsePage.tsx`: 행마다 EyeOff 차단 아이콘(★ 왼쪽) + 툴바에 "차단 회사 관리" 버튼(스크랩 탭 왼쪽) + BlacklistManagerModal
   - 블랙 회사 목록 로드·차단·해제/정규화(`normCompany`) 로컬 구현, rows에서 차단 회사 필터링
   - resume용 `BlacklistManagerModal.tsx` 신규 (scraper와 동일 UI, 자체 BlacklistItem 타입)
   - 프론트 빌드(tsc -b && vite build) 성공
8. **차단 취소해도 등록되는 버그 수정 + 차단 전용 커스텀 다이얼로그** (scraper + resume frontend)
   - 버그 원인: `window.prompt(...) ?? ""`가 null(취소)을 `""`로 바꿔 `if (reason === null) return`이 항상 false → 취소/빈 입력에도 차단 등록됨
   - 신규 공용 컴포넌트 `BlockConfirmDialog.tsx` (scraper + resume 각각) — 확인+사유 입력+취소 버튼 있는 Tailwind 모달
   - `blockCompany`는 `setBlockDialog(company)`로 다이얼로그만 열고, `confirmBlock(company, reason)`이 실제 차단 수행
   - `alert(...)` 성공/실패 안내 → 컴포넌트 내 `showNotice`(3초 자동 소멸 인라인 배지)로 교체
   - 적용: Viewer.tsx / Search.tsx / PostingsBrowsePage.tsx (차단 관련만 우선 적용, 나머지 confirm 교체는 보류)
   - 프론트 빌드(tsc -b && vite build) 성공 (scraper + resume)
9. **공용 UI 패키지 `packages/ui-shared` 도입 (MSA 전면 중앙화)** (신규)
   - 핵심 설계: 공용 패키지는 **소스로 직접 참조** (npm 별도 설치 없이 Vite alias + tsconfig paths로 각 앱이 빌드)
   - 전역 다이얼로그 오버라이드: `initGlobalDialogs()`가 `window.alert/confirm/prompt`를 커스텀으로 대체,
     `DialogHost`(alert/confirm/prompt 호스트) + 공용 `BlockConfirmDialog`/`BlacklistManagerModal` 제공
   - scraper/resume/platform/auth **4개 프론트 전부 연동** (vite alias + tsconfig paths + index.css `@source` + main.tsx 마운트)
   - 로컬 복붙이던 `BlacklistManagerModal`/`BlockConfirmDialog`(scraper/resume 각각) 삭제 → 공용으로 통합
   - **전 프론트 `confirm()` 호출부를 `await`+async로 전환** (Promise가 truthy로 판정돼 항상 확정되던 버그 방지):
     - resume: ResumesPage, PostingsBrowsePage, ApplicationsPage, CrudSection
     - scraper: Schedule:380, Viewer:354
     - platform: AccountSettings, AdminSessions, AdminTenants, AdminUsers
     - auth: alert만 있어서 호출부 수정 불필요 (오버라이드로 자동 적용)
   - 4개 프론트 `npm run build`(tsc -b && vite build) 전부 성공

## 변경된 파일 목록
- `platform/frontend/src/api/dashboard.ts`: ScrapItemDto.deadline + deadline today 유틸
- `platform/frontend/src/pages/Dashboard.tsx`: 마감임박 정렬 + D-N 배지
- `docs/learnings/007-260828-tailscale-learning.md`: 신규 (Tailscale 개념)
- `modules/resume/frontend/src/pages/EditPage.tsx`: 우측 목차 네비 + 섹션 편성(포함/순서) + inline 전달
- `modules/resume/frontend/src/components/CrudSection.tsx`: `inline` prop + 인라인 편집형 렌더링
- `modules/resume/frontend/src/pages/ResumesPage.tsx`: 다중 이력서 정체성 안내 문구 + 불러오기 라벨
- `modules/scraper/backend/.../controller/JobPostingController.java`: 블랙 필터 분기가 날짜/run/사이트 조건 존중하도록 수정
- `modules/scraper/frontend/src/components/BlacklistManagerModal.tsx`: 신규 공용 모달 컴포넌트
- `modules/scraper/frontend/src/pages/Viewer.tsx`: 차단 회사 관리 인라인 패널 → 모달 + EyeOff 버튼 ★ 왼쪽으로 이동
- `modules/scraper/frontend/src/pages/Search.tsx`: 차단 회사 관리 인라인 패널 → 모달
- `modules/resume/frontend/src/components/BlacklistManagerModal.tsx`: 신규 (resume용 동일 모달)
- `modules/resume/frontend/src/pages/PostingsBrowsePage.tsx`: 공고탐색에 차단 기능 추가 (EyeOff 행 아이콘 + 차단 관리 버튼 + 모달 + 블랙 필터)
- `modules/scraper/frontend/src/components/BlockConfirmDialog.tsx`: 신규 차단 커스텀 다이얼로그
- `modules/resume/frontend/src/components/BlockConfirmDialog.tsx`: 신규 차단 커스텀 다이얼로그
- `modules/scraper/frontend/src/pages/Viewer.tsx`: 차단 prompt → BlockConfirmDialog + showNotice (취소 버그 수정)
- `modules/scraper/frontend/src/pages/Search.tsx`: 차단 prompt → BlockConfirmDialog + showNotice (취소 버그 수정)
- `modules/resume/frontend/src/pages/PostingsBrowsePage.tsx`: 차단 prompt → BlockConfirmDialog + showNotice (취소 버그 수정)
- `packages/ui-shared/`: 신규 공용 UI 패키지 (store.ts, initGlobalDialogs.ts, DialogHost.tsx, BlockConfirmDialog.tsx, BlacklistManagerModal.tsx, index.ts, package.json, tsconfig.json)
- `modules/scraper/frontend/{vite.config.ts,tsconfig.app.json,src/index.css,src/main.tsx}`: 공용 패키지 연동 (alias/paths/@source/DialogHost)
- `modules/scraper/frontend/src/pages/{Viewer,Search,Schedule}.tsx`: confirm→await 전환 + 로컬 컴포넌트 import → @sh-platform/ui 교체
- `modules/scraper/frontend/src/components/{BlockConfirmDialog,BlacklistManagerModal}.tsx`: 삭제 (공용으로 통합)
- `modules/resume/frontend/{vite.config.ts,tsconfig.app.json,src/index.css,src/main.tsx}`: 공용 패키지 연동
- `modules/resume/frontend/src/pages/{ResumesPage,PostingsBrowsePage,ApplicationsPage}.tsx` + `components/CrudSection.tsx`: confirm→await 전환 + 공용 import 교체
- `modules/resume/frontend/src/components/{BlockConfirmDialog,BlacklistManagerModal}.tsx`: 삭제 (공용으로 통합)
- `platform/frontend/{vite.config.ts,tsconfig.app.json,src/index.css,src/main.tsx}`: 공용 패키지 연동
- `platform/frontend/src/pages/AccountSettings.tsx` + `pages/admin/{AdminSessions,AdminTenants,AdminUsers}.tsx`: confirm→await 전환
- `modules/auth/frontend/{vite.config.ts,tsconfig.app.json,src/index.css,src/main.tsx}`: 공용 패키지 연동 (최초 로컬 생성 → npm install 후 빌드)
- `docs/guides/011-260828-ui-shared-guide.md`: 신규 (공용 패키지 사용·연동·문제해결 가이드)
- `docs/errors/012-260828-ui-shared-react-build-error.md`: 신규 (경로·react alias·auth tsc 오류 기록)

## 커밋 내역
| 커밋 | 내용 |
|------|------|
| f46a592 | feat: 대시보드 '내 스크랩' 마감임박(D-3 이내) 상단 노출 + D-N 배지 |
| 3adb383 | docs: Tailscale 개념 학습 기록 (007, WireGuard 기반 오버레이 네트워크) |
| 41cf8e7 | feat: 이력서 벤치마킹 Phase3/4 — 목차 네비·섹션 토글, 자기소개 인라인, 다중 이력서 문구 |
| 4133c34 | fix: 뷰어 날짜/시간 구분 회귀 버그 수정 (블랙리스트 필터 분기가 날짜/run 조건 무시) |
| 113471b | feat: 차단 회사 관리 인라인 패널 → 모달 팝업 전환 (scraper Viewer/Search) |
| 10bad24 | docs: 작업 일지 갱신 |
| 2b91224 | feat: 공용 UI 패키지(packages/ui-shared) 도입 — 전역 alert/confirm/prompt 오버라이드(DialogHost) + 4개 프론트 연동 + confirm await 전환 |

## 이슈 및 해결
- **git 사용자 정보 미설정**: 이 PC에서 commit 시 "Author identity unknown". 기존 커밋과 동일 저자
  (`SungHoon kim <110158529+SungHoonGit@users.noreply.github.com>`)를 repo-로컬 git config로 설정.
- **이 PC에 Java 미설치**: `gradlew.bat` 실행 시 JAVA_HOME 없음 → 로컬 백엔드 테스트 불가.
  이번 변경은 백엔드(JobPostingController) 포함이므로 백엔드 컴파일/테스트는 CI(GitHub Actions)에서 수행될 예정.
  사용한 Repository 메서드(`findByConfigIdAndCrawlLogIdIn` 등)는 모두 존재·동일 시그니처 확인으로 컴파일 가능성 높음. (프론트는 로컬 빌드로 검증 완료)
- **npm install로 package-lock.json 변경**: 설치 환경 차이로 발생한 비의도적 변경이라 원복 후 커밋.
- **차단 취소해도 등록되는 버그**: `window.prompt(...) ?? ""`가 취소(null)를 빈 문자열로 치환 →
  `if (reason === null) return` 무력화. 커스텀 `BlockConfirmDialog`로 교체하며 취소/빈 사유 분기 명확화.

## 향후 작업
1. 배포 후 UI 확인: 대시보드 "내 스크랩" D-N 배지, 이력서 편집 우측 목차/섹션 토글/자기소개 인라인 편집
2. (참고) resume Phase 3 도메인은 전부 구현 완료 상태 (이전 todo outdated — education/skill/certificate/project/introduction/portfolio-item 모두 존재)
3. 배포 후 차단 다이얼로그 확인: 취소하면 차단되지 않는지, 차단/해제 notice 표시 (Viewer/Search/공고탐색)
4. 배포 후 공용 다이얼로그 확인: 커스텀 alert/confirm/prompt가 4개 앱에서 모두 동작하는지 (특히 confirm 취소/확인 분기, prompt 입력)
5. (완료) 나머지 confirm/alert 교체 → 공용 패키지 `@sh-platform/ui` + 전역 오버라이드로 전부 자동 적용됨

## 주요 참조 파일
- 설계: `docs/plans/021-260827-resume-benchmark-design.md` (Phase 3/4)
- 학습: `docs/learnings/007-260828-tailscale-learning.md`
