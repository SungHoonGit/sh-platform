# 2026-09-14 작업 일지

## 작업 환경
- 날짜: 2026-09-14 (월)
- 시간: 13:00 기준
- 브랜치: master

## 오늘 완료된 작업
1. **업무 파악**: `git pull`로 주말 커밋 6개(903aeb9~957083e) 동기화 — 이력서 아이템 documentId 분리·DDL v13·403 토큰 자동 갱신·제목 인라인 수정·displayOrder 보존. `docs/daily/2026-09-13-work-log.md` 확인.
2. **/resume 404 오류 진단**: resume.jar 프로세스는 정상 기동인데 SPA가 안 열리는 문제.
   - 확정 원인: **배포된 jar에 프론트 정적 리소스(index.html)가 없음.** 백엔드 빌드 시 `frontend/dist`가 없어 `copyFrontendDist`가 빈 `static/`으로 jar 구성됨.
   - 근거: `/resume/index.html`·`/resume/favicon.svg` 전부 404 JSON, `/resume/api/v1/documents` 401(신규 컨트롤러 존재 = 새 백엔드 코드 기동 중), `GlobalExceptionHandler`가 `NoResourceFoundException`을 잡아 `{"code":"NOT_FOUND"}` 반환(common `GlobalExceptionHandler.java:54`).
   - **404 JSON vs 502 에러페이지 구분**: 502는 nginx가 백엔드에 연결 실패 시 자체 `502.html`로 대체. 이번엔 앱이 살아서 요청을 받고 404를 응답 → nginx가 그대로 통과.
3. **복구 배포**: QUEUED 상태로 멈춰 있던 런(34748835094) 정리 후 `workflow_dispatch` 재배포(run 34805245347, 5m26s 성공). 서버에서 프론트 4종 빌드 → 프론트 포함 jar 재빌드 → 서비스 재시작.
4. **로컬 사전 검증**: resume 프론트 `npm run build` OK(static 생성), `./gradlew :modules:resume:backend:test` BUILD SUCCESSFUL.
5. **복구 확인**: `/resume/` 200 SPA 서빙, 번들 `index-CF9mV7v2.js`에 `documentId`·`refreshToken`·`displayOrder`·`hiddenItemIds` 포함 확인(주말 코드 반영됨).
6. **MySQL 주기 백업 구현·배포** (`5cd80e8`): `scripts/backup-mysql.sh`(3개 DB mysqldump+gzip+7일 보존) + `infra/cron.d/sh-platform-mysql-backup`(매일 03:30 KST) + 설치/복구 가이드(`docs/guides/009-260914-mysql-backup-guide.md`). CI 배포가 cron.d·권한·백업디렉터리를 자동 설치(멱등). 배포 런 34808496186에서 설치 단계 실행 확인.
7. **백업 생명주기(PITR) 완성** (`5fd001e`): 백업 스크립트가 `SHOW MASTER STATUS`로 binlog 좌표를 `binlog-status.txt`에 기록 + 가이드에 PITR 복구 절차(mysqlbinlog 재생)·binlog 보존 설정(`binlog_expire_logs_seconds` 14일 권장) 추가. `scripts/**`를 배포 트리거에 포함(스크립트 변경 시 자동 배포). 배포 런 34813151423 성공. (Oracle archive log ↔ MariaDB binlog 유사성 정리)
8. **백업 실동작 검증** (`c66daac`): 배포 진단에 DB·백업 상태 확인 단계 추가 + `.first-run-done` 마커로 첫 배포에서 백업 즉시 1회 실행. 결과: 3개 DB 덤프 성공(`/home/ubuntu/backups/mysql/20260914/`, sh_pass 24K·scraper_platform 2.0M·resume_platform 20K), binlog ON·보존 10일(864000s) 확인, cron.d 설치 확인. **단, binlog 좌표는 `sh_user`에 `BINLOG MONITOR` 부족으로 disabled 기록** — root 원격 무비밀번호 접근도 차단됨(정상). 부여는 DB 호스트에서 관리자로 1회 필요(`GRANT BINLOG MONITOR ON *.* TO 'sh_user'@'%';`).
9. **BINLOG MONITOR 부여 완료** (`82d6c20`): DB 서버(`161.33.138.23`, A1.Flex)에서 `sudo mysql`로 확인 — sh_user는 실제 `'sh_user'@'10.0.0.%'`로 등록(처음 `'%'`로 GRANT 시 "Can't find any matching row"). `GRANT BINLOG MONITOR ON *.* TO 'sh_user'@'10.0.0.%'` + `SET GLOBAL binlog_expire_logs_seconds=1209600`(14일) 적용 완료. **Grants 확인 중 `portfolio_platform` DB 존재 발견 → 백업 대상 4개로 확장.** 배포 런에서 `[coordinate OK]` = 웹 서버에서 좌표 조회 성공 확인.
10. **웹/WAS 파일 백업 추가·검증** (`33742d7`): `scripts/backup-files.sh`(data·uploads tar.gz, 03:35 KST, 7일 보존) cron.d 2라인 등록. 첫 배포 1회 실행 검증: data 524K + uploads 924K 생성 성공. 실행 로그 일자별 분리(`backup-YYYYMMDD.log`), 스크립트에 `TZ=Asia/Seoul` 지정(서버 UTC 보정, `172c97b`).

## 변경된 파일 목록
- 코드 변경 없음(운영 복구만 수행)
- `docs/daily/2026-09-14-work-log.md`: 작성(본 파일)

## 이슈 및 해결
- **이력서 프로세스는 살아있는데 SPA 404**: 배포 jar에 프론트 static 누락이 근본 원인. 프로세스/nginx/DB는 정상. 해결은 프론트 포함 재빌드·재배포.
- **멈춘 CI 런**: `be5addc` 커밋 기반 런(34748835094)이 QUEUED 15h+ 잡 미생성 → GitHub에서 자동 정리됨.

## 커밋 내역
| 커밋 | 내용 |
|------|------|
| 3b74740 | docs: 2026-09-14 작업 일지 작성 + AGENTS 상태 갱신 |
| 5cd80e8 | feat(infra): MySQL 주기 전체 백업 — mysqldump + cron.d(03:30 KST, 7일 보존) + 가이드 |
| 5fd001e | feat(infra): 백업 생명주기 완성 — binlog 좌표 기록 + PITR 복구 절차/보존 가이드, scripts/** 트리거 |
| f2cfca7 | chore(ci): 배포 진단 로그에 MySQL binlog/백업 상태·cron.d 확인 단계 추가 |
| c66daac | chore(ci): BINLOG MONITOR 자동 부여 시도(실패) + 첫 배포 시 백업 즉시 실행 검증(성공) |
| adf4738 | feat(infra): 처리할 수 있는 것 완료 — DB 관리자 1회 스크립트, MYSQL_ADMIN_PASS secret 자동 적용, AGENTS 갱신 |
| 82d6c20 | fix(infra): 백업 대상 portfolio_platform 추가(4개 DB) — Grants 확인으로 누락 발견 |
| 0a997f6·33742d7·62d6a34·172c97b | feat(infra): 웹/WAS 파일 백업(data·uploads) 추가 + 로그 일자별 분리 + TZ=KST + 첫 배포 검증 |

## 향후 작업
1. **실사용 검증** (사용자 진행 중): 문서별 독립성(한 문서 수정 시 타 문서 영향 없음), 불러오기 시 아이템 복제, 제목 인라인 수정, 403 자동 토큰 갱신.
2. **백업 정기 실행 확인**: 내일 03:30(DB)·03:35(파일) KST 크론 산출물 — 4개 DB 덤프 + `binlog-status.txt` 실좌표 + `backup-YYYYMMDD.log` 확인.
3. **미저장 입력 재입력**: 인터엑스(문서 12)의 "자바 3년차" 등 마이그레이션 전 미저장 편집분.