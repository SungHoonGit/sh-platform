# 2026-08-30 작업 일지

## 작업 환경
- 날짜: 2026-08-30 (일)
- 시간: 세션 기준
- 브랜치: master

## 오늘 완료된 작업
1. **대시보드 '내 스크랩' 레이아웃 개선: 사이트/날짜 우측 배열** (platform/frontend)
   - 사용자 요청: "내 스크랩에서 잡코리아/날짜를 오른쪽에 배열해 지원관리 처럼"
   - `Dashboard.tsx` 내 스크랩 `<li>`를 `flex items-center justify-between gap-3`로 변경
   - 좌측: ★ + 회사명 + 포지션 + 마감뱃지(deadlineBadge), 우측(`shrink-0 text-right`): 사이트명 배지 + 날짜(fmtDate)
   - 타입체크(tsc -b) 통과
2. **차단 사유 카테고리화: DB 마스터 + 검색 API 자동완성** (scraper backend + packages/ui-shared)
   - 사용자 요청: "차단 사유 등록을 카테고리화 해서 자동완성/선택" (Notion/Jira식), 하드코딩 지양 원칙 → **DB 마스터로 확정**
   - 신규 `block_reasons` 마스터 테이블 + 멱등 시드 (`docs/scraper/ddl-v7.sql`) — 초기 프리셋 사유 7개
   - 백엔드: `BlockReason` Entity / `BlockReasonRepository` / `BlockReasonService`(검색) +
     `CompanyBlacklistController.get /company-blacklist/reasons/search?q=` 추가 (활성 사유 Top20, sort_order 정렬)
   - 프론트: 공용 `BlockConfirmDialog.tsx`에 **디바운스(250ms) 자동완성 검색 + 선택 드롭다운** 추가
     (3개 앱인 scraper Viewer/Search + resume 공고탐색에서 동일하게 적용)
   - `BlockReasonServiceTest` 신규 (검색/빈 키워드 케이스) → `./gradlew :modules:scraper:backend:test` 통과
   - scraper/resume/platform 프론트 `tsc -b` 전부 통과
3. **배포 워크플로우에 DDL v7 추가** (.github/workflows/deploy-backend.yml)
   - scraper ddl-v6.sql 다음에 `docs/scraper/ddl-v7.sql` 실행 라인 추가
   - scraper가 `ddl-auto: validate` 이므로 재시작 전 DDL 선실행 필수 → 배포 시 block_reasons 테이블 자동 생성
4. **차단 사유 → 복수 카테고리(다대다) 확장: 회사유형 + 사유** (사용자 요청 반영)
   - 요구: "스타트업 X" 등 카테고리를 **여러 개** 등록하고 싶다 + 추천에 스타트업이 안 뜸 + 데이터 마이닝 가능성
   - 결정(질문 응답): **연결 테이블(다대다)** + 회사유형/사유 카테고리 둘 다 시드 + 목록 태그 배지
   - DB (`docs/scraper/ddl-v8.sql`): `block_reasons`에 `category` 컬럼(company_type/reason) 멱등 ALTER,
     회사유형 시드 8개(스타트업 X·스타트업·대기업·중견기업·외국계·공공·아웃소싱·블라인드),
     연결 테이블 `blacklist_block_reason`(FK+CASCADE). 서버에 직접 적용해 검증 완료.
   - 백엔드: `CompanyBlacklist`에 `@ManyToMany blockReasons`(join table),
     `CompanyBlacklistService.add(company, reason, reasonIds)`로 카테고리 저장(멱등 갱신),
     `AddRequest.reasonIds` + `GET /company-blacklist/reasons`(전체) 추가. 기존 자유문자 `reason`은 **메모로 유지**(기존 17건 데이터 보존).
   - 프론트: 공용 `BlockConfirmDialog`를 **회사유형/사유 체크박스 다중 선택 + 자유 메모**로 전환(디바운스 자동완성 제거),
     `BlacklistManagerModal`에 카테고리 **태그 배지**(회사유형=violet/사유=slate), 호출부 3곳(Search·Viewer·PostingsBrowsePage)에 reasonIds 전달.
    - 테스트: `CompanyBlacklistServiceTest`(카테고리 저장/갱신/빈 목록), `BlockReasonServiceTest` → 전부 통과. 프론트 3개 `tsc -b` 통과.
5. **차단 카테고리 UX 재정의: 체크박스 → 자유입력 + 제안 드롭다운(Notion 태그식)** (사용자 요청 반영)
   - 사용자 요청: "체크박스가 아니라 **input에 '스타트업' 입력 → 카테고리화 되며 이전에 입력했던 것들이 조회로 나오는** 간략한 방식"
   - 결정(질문 응답): 기존 DB 구조(마스터+연결테이블+다대다)는 유지, **UI만 자유입력+제안으로 단순화**, 복수 등록 유지
   - 프론트 `BlockConfirmDialog`: 회사유형/사유 체크박스 그룹 제거 → **자유 input + 디바운스 검색 제안 드롭다운 + 태그 칩(복수)**.
     Enter/선택으로 태그 추가, Backspace로 제거, 자유 메모 필드 유지.
   - 백엔드 신규 카테고리 자동 승격: 사용자가 Enter로 입력한 새 카테고리를
     `BlockReasonService.ensureCategory(name)`이 `block_reasons(category='user')`로 **멱등 upsert** →
     "이전에 입력했던 카테고리가 다음에도 제안으로 나오는" 요구 구현.
     `CompanyBlacklistService.add(company, reason, reasonIds, categoryNames)`, `AddRequest.categoryNames` 추가.
    - 테스트: `CompanyBlacklistServiceTest`에 사용자 카테고리 승격 케이스 추가 → 전부 통과. 프론트 3개 `tsc -b` 통과.
6. **차단 다이얼로그 단순화(메모 제거) + 차단 리스트 수정 기능 추가** (사용자 요청 반영)
   - 결정(질문 응답): ① 자유 메모 필드 제거(DB reason 컬럼/기존 데이터는 보존), ② 차단 리스트에 **수정 기능 추가**
   - `BlockConfirmDialog`: 자유 메모 input 제거, 수정 모드용 `initialTags`(기존 카테고리 프리필) + `confirmLabel`("수정") props 추가.
     add/update 공용으로 사용.
   - 백엔드: `CompanyBlacklistService.update(accountId, id, reasonIds, categoryNames)`(본인 항목만, 메모 보존) +
     Controller `PUT /company-blacklist/{id}` + `UpdateRequest`. `remove` 이전에 배치.
   - 프론트: `scraper.ts` `updateBlacklist(id, reasonIds, categoryNames)`, `BlacklistManagerModal`에 `onEdit` + "수정" 버튼,
     호출부 3곳(Search·Viewer·PostingsBrowsePage)에 `editTarget` 상태 + `confirmEdit` + 편집용 BlockConfirmDialog 연결.
   - 테스트: `CompanyBlacklistServiceTest`에 update(교체+메모보존 / 타인 ignored) 케이스 추가 → 전부 통과. 프론트 3개 `tsc -b` 통과.

## 변경된 파일 목록
- `platform/frontend/src/pages/Dashboard.tsx`: 내 스크랩 아이템 — 사이트명 배지/날짜를 우측(`justify-between`)으로 배열
- `docs/scraper/ddl-v7.sql`: 신규 — `block_reasons` 마스터 테이블 + 멱등 시드(사유 7개)
- `modules/scraper/backend/.../model/BlockReason.java`: 신규 — 차단 사유 마스터 Entity (name/sort_order/active)
- `modules/scraper/backend/.../repository/BlockReasonRepository.java`: 신규 — 이름 포함+활성 검색 조회
- `modules/scraper/backend/.../service/BlockReasonService.java`: 신규 — (질의형) 활성 사유 검색
- `modules/scraper/backend/.../controller/CompanyBlacklistController.java`: `GET /company-blacklist/reasons/search?q=` 추가
- `modules/scraper/backend/src/test/.../service/BlockReasonServiceTest.java`: 신규 — 검색/빈 키워드 테스트
- `packages/ui-shared/src/BlockConfirmDialog.tsx`: 사유 입력 시 디바운스 자동완성 + 선택 드롭다운 추가
- `.github/workflows/deploy-backend.yml`: ddl-v7.sql 실행 라인 추가
- `docs/scraper/ddl-v8.sql`: 신규 — block_reasons.category 멱등 ALTER + 회사유형 시드 + blacklist_block_reason 연결 테이블
- `modules/scraper/backend/.../model/BlockReason.java`: `category` 컬럼 추가
- `modules/scraper/backend/.../model/CompanyBlacklist.java`: `@ManyToMany blockReasons` (join table) 추가
- `modules/scraper/backend/.../service/CompanyBlacklistService.java`: `add(company, reason, reasonIds)` 카테고리 저장(멱등 갱신)
- `modules/scraper/backend/.../controller/CompanyBlacklistController.java`: `AddRequest.reasonIds` + `GET /company-blacklist/reasons`(전체) 추가
- `modules/scraper/backend/.../repository/BlockReasonRepository.java`: `findAllByActiveTrueOrderBySortOrderAsc` 추가
- `modules/scraper/backend/src/test/.../service/CompanyBlacklistServiceTest.java`: 신규 — 카테고리 저장/갱신/빈 목록
- `modules/scraper/frontend/src/api/scraper.ts`: BlacklistItem.blockReasons + addBlacklist(reasonIds) + fetchBlockCategories
- `modules/scraper/frontend/src/pages/{Search,Viewer}.tsx`: confirmBlock(reason, reasonIds) 연동
- `modules/resume/frontend/src/pages/PostingsBrowsePage.tsx`: BlacklistItem.blockReasons + confirmBlock(reasonIds) 연동
- `packages/ui-shared/src/BlockConfirmDialog.tsx`: 자동완성 → 회사유형/사유 체크박스 다중 선택 + 자유 메모로 전환
- `packages/ui-shared/src/BlacklistManagerModal.tsx`: 카테고리 태그 배지(회사유형/사유) 표시
- `modules/scraper/backend/.../service/BlockReasonService.java`: `ensureCategory(name)` 신규 — 사용자 신규 카테고리 마스터 upsert(category='user')
- `modules/scraper/backend/.../repository/BlockReasonRepository.java`: `findByName`, `findTopByOrderBySortOrderDesc` 추가
- `modules/scraper/backend/.../service/CompanyBlacklistService.java`: `add(company, reason, reasonIds, categoryNames)` — categoryNames 자동 승격+병합
- `modules/scraper/backend/.../controller/CompanyBlacklistController.java`: `AddRequest.categoryNames` 추가
- `packages/ui-shared/src/BlockConfirmDialog.tsx`: 체크박스 그룹 제거 → 자유입력+제안 드롭다운+태그 칩(복수)으로 재구성
- `modules/scraper/frontend/src/api/scraper.ts`: `addBlacklist(company, reasonIds, reason, categoryNames)` 추가
- `modules/scraper/frontend/src/pages/{Search,Viewer}.tsx` + `modules/resume/frontend/src/pages/PostingsBrowsePage.tsx`: `onConfirm(reason, reasonIds, categoryNames)` 연동
- `modules/scraper/backend/src/test/.../service/CompanyBlacklistServiceTest.java`: 사용자 카테고리 승격 테스트 추가
- `modules/scraper/backend/.../service/CompanyBlacklistService.java`: `update(accountId, id, reasonIds, categoryNames)` 추가(메모 보존)
- `modules/scraper/backend/.../controller/CompanyBlacklistController.java`: `PUT /company-blacklist/{id}` + `UpdateRequest` 추가
- `modules/scraper/backend/src/test/.../service/CompanyBlacklistServiceTest.java`: update(교체+메모보존/타인 ignored) 테스트 추가
- `modules/scraper/frontend/src/api/scraper.ts`: `updateBlacklist(id, reasonIds, categoryNames)` 추가
- `packages/ui-shared/src/BlockConfirmDialog.tsx`: 자유 메모 input 제거 + `initialTags`(수정 프리필)/`confirmLabel` props 추가
- `packages/ui-shared/src/BlacklistManagerModal.tsx`: `onEdit` prop + 각 행 "수정" 버튼 추가
- `modules/scraper/frontend/src/pages/{Search,Viewer}.tsx` + `modules/resume/frontend/src/pages/PostingsBrowsePage.tsx`: `editTarget` 상태 + `confirmEdit` + 편집용 BlockConfirmDialog 연결

## 커밋 내역
| 커밋 | 내용 |
|------|------|
| b2fbdc5 | feat: 대시보드 '내 스크랩' 사이트/날짜 우측 배열 + 차단 사유 카테고리화(block_reasons DB 마스터 + 검색 API 자동완성) |
| dea7118 | fix: ddl-v7 시드 유도 테이블 숫자 리터럴 컬럼라이블 충돌(Duplicate column name '1') — 명시적 alias 부여 |
| 115ea0d | feat: 차단 사유 복수 카테고리(다대다) 확장 — 회사유형+사유, BlockConfirmDialog 다중선택, 목록 태그 배지 |
| 9b9f589 | feat: 차단 카테고리 UX를 자유입력+제안 드롭다운(Notion 태그식)으로 변경 + 신규 입력 마스터 자동 승격(ensureCategory) |
| 816bcbd | fix: 차단 신규 등록 500(UnsupportedOperationException) — 불변 리스트 병합 충돌을 가변 ArrayList+단일 save로 해결 |
| (이번)  | feat: 차단 다이얼로그 메모 필드 제거 + 차단 리스트 수정 기능 추가(PUT, 수정용 BlockConfirmDialog) |

## 이슈 및 해결
- **배포 DDL 실패: `ERROR 1060 Duplicate column name '1'` (docs/scraper/ddl-v7.sql:21)**
  - 원인: 멱등 시드의 유도 테이블 `(SELECT '사유', 1, 1) AS t`에서 두 개의 숫자 리터럴 `1`이
    자동 생성 컬럼 라벨 `1`로 충돌 → MySQL "Duplicate column name '1'" 발생. (CREATE TABLE은 성공,
    첫 INSERT에서 중단) — resume DDL은 서로 다른 문자열 리터럴이라 이 문제가 없었음.
  - 해결: 유도 테이블 내부에 명시적 컬럼 별칭 부여 `(SELECT '사유' AS name, 1 AS sort_order, 1 AS active) AS t`.
  - 재배포로 ddl-v7 재실행 (block_reasons 테이블은 이미 생성, 시드는 멱등이라 안전).
- **차단 신규 등록 500: `java.lang.UnsupportedOperationException` (CompanyBlacklistService.add)**
  - 원인: `resolveCategories`가 `Stream.toList()`로 만든 **불변 리스트**를 `setBlockReasons`에 할당한 뒤
    두 번째 `repository.save`(merge)에서 Hibernate가 컬렉션에 `.add()`하려다 실패.
  - 해결: 신규 등록 엔티티에 `new ArrayList<>(categories)`로 빌드하고 **단일 save**로 변경.
    `resolveCategories`도 가변 `ArrayList` 반환으로 변경. (fix 커밋 816bcbd)

## 향후 작업
1. 배포 후 확인: block_reasons 마스터 생성(category 포함)/연결 테이블 + `/company-blacklist/reasons`(전체) 동작
2. 배포 후 UI 확인: 차단 다이얼로그에서 **자유 입력 + 제안 드롭다운 + 복수 태그** 동작, 새로 입력한 카테고리가
   다음 차단 시 제안으로 나오는지 (user 카테고리 자동 승격)
3. **데이터 마이닝(예정)**: block_reasons 카테고리별 차단 통계(회사유형별 비중, 사유 상위 등)를 대시보드에 제공.
   카테고리가 숫자 id로 코드화되어 있어 JOIN/집계로 바로 분석 가능 (하드코딩 지양 원칙 부합)
