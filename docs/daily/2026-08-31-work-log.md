# 2026-08-31 작업 일지

## 작업 환경
- 날짜: 2026-08-31 (월)
- 브랜치: master

## 오늘 완료된 작업
1. **블랙리스트 카테고리별 차단 통계 (데이터 마이닝) — Phase 7 완료** (022 설계 문서)
   - 백엔드: `GET /scraper/company-blacklist/stats` 신규 — 내 블랙리스트의 카테고리(회사유형/사유/사용자 입력)별 사용 빈도 집계
     - `CompanyBlacklistService.stats()` — name 기준 멱등 집계 (block_reasons.name UNIQUE), 카테고리 없는(레거시 자유 메모) 항목은 uncategorized로 분리
     - 응답: `{ total, uncategorized, categories: [{id, name, category, count}] }` — count 내림차순 + name 오름차순
     - `CompanyBlacklistRepository`: `@Query` DISTINCT + `LEFT JOIN FETCH b.blockReasons`로 N+1 방지
     - 테스트: 집계(카테고리 빈도/uncategorized), 빈 목록 2건 추가 → 전체 스크래퍼 테스트 통과
   - 프론트(platform 대시보드): "차단 회사 유형"(violet 바) + "차단 사유"(slate 바) 2개 카드 신규
     - `CategoryBars` 공용 렌더, 차단 N곳·카테고리 없음 N곳 서브 정보
     - `Raw` fetch: `/scraper/company-blacklist/stats` (기존 `getRaw` 재사용, Authorization Bearer)
     - 수집·지원·스크랩 목록 아래 전용 섹션으로 배치, 에러 상태에 포함
     - `tsc -b` + `vite build` 성공

## 변경된 파일 목록
- `modules/scraper/backend/.../repository/CompanyBlacklistRepository.java`: LEFT JOIN FETCH DISTINCT 쿼리 (N+1 방지)
- `modules/scraper/backend/.../service/CompanyBlacklistService.java`: `stats()` + `BlockStatsResponse` record 신규
- `modules/scraper/backend/.../controller/CompanyBlacklistController.java`: `GET /company-blacklist/stats` 추가
- `modules/scraper/backend/src/test/.../service/CompanyBlacklistServiceTest.java`: stats/normalizedNames 테스트 추가
- `platform/frontend/src/api/dashboard.ts`: `BlockStats`/`fetchBlockStats` 신규
- `platform/frontend/src/pages/Dashboard.tsx`: 차단 회사 유형/차단 사유 카드 + `CategoryBars` 컴포넌트 추가
- `docs/plans/022-260830-company-blacklist-category-design.md`: Phase 7 완료 표시

## 이슈 및 해결
- `Comparator.comparingInt` 사용 시 `count(long)` 메서드 레퍼런스 불일치 → `comparingLong`으로 수정
- 통계 집계 키를 `reason.id`로 하니 테스트 `BlockReason.of()`가 id 미설정(null)이라 한 건으로 병합되는 문제
  → `name`(UNIQUE) 기준으로 키잉. 실데이터에도 동일 카테고리는 name이 같으므로 집계 결과 동일.

## 커밋
| 커밋 | 내용 |
|------|------|
| (pending) | feat: 블랙리스트 카테고리별 차단 통계 API + 대시보드 차단 회사 유형/사유 |

## 향후 작업
1. 학교 데이터셋 확장 (고등학교/대학원 목록 추가 + schools/majors 마스터 시드 확충)
2. Redis 유령 세션 lazy cleanup 배포 검증 + A-2 기기별 로그아웃
3. AGENTS.md 진행 중인 작업 배너 갱신 (Redis → 최근 블랙리스트/대시보드 작업 기준)