# 2026-08-31 작업 일지

## 작업 환경
- 날짜: 2026-08-31 (월)
- 브랜치: master

## 오늘 완료된 작업
1. **블랙리스트 카테고리별 차단 통계 (데이터 마이닝) — Phase 7 완료** (022 설계 문서)
   - 백엔드: `GET /scraper/company-blacklist/stats` 신규 — 내 블랙리스트의 카테고리(회사유형/사유/사용자 입력)별 사용 빈도 집계
     - `CompanyBlacklistService.stats()` — name 기준 멱등 집계 (block_reasons.name UNIQUE), 카테고리 없는(레거시 자유 메모) 항목은 uncategorized로 분리
     - 응답: `{ total, uncategorized, categories: [{id, name, category, count}] }` — count 내림차순 + name 오름차순
     - `CompanyBlacklistRepository`: `@Query` DISTINCT + `LEFT JOIN FETCH b.blockReasons`로 N+1 방지
     - 테스트: 집계(카테고리 빈도/uncategorized), 빈 목록 2건 추가 → 전체 스크래퍼 테스트 통과
   - 프론트(platform 대시보드): "차단 회사 유형"(violet 바) + "차단 사유"(slate 바) 2개 카드 신규
     - `CategoryBars` 공용 렌더, 차단 N곳·카테고리 없음 N곳 서브 정보
     - `Raw` fetch: `/scraper/company-blacklist/stats` (기존 `getRaw` 재사용, Authorization Bearer)
     - 수집·지원·스크랩 목록 아래 전용 섹션으로 배치, 에러 상태에 포함
     - `tsc -b` + `vite build` 성공
   - 커밋: `6515184`
2. **학교 데이터셋 확장 (고등학교/대학원/전공 마스터)** — `docs/resume/ddl-resume-v7.sql`
   - '고등학교' 유형이 전무했던 문제 보완: 서울/경기/대전충청/영남/호남/강원/제주 + 특목고 고등학교 88종
   - 대학원 38종(수도권/지역/IST 계열) + 대전·충청 중심 대학교 13종 + 전공 30종
   - 복합 UNIQUE `(name, school_type)` 충돌 없음, 멱등 INSERT 패턴, 중복명 0건
   - 배포 워크플로우 `deploy-backend.yml`에 v7 DDL 라인(v6 다음) 추가 — pull 요청 시 docs/**/*.sql 트리거로 자동 실행
   - 커밋: `056420b`
3. **Redis 유령 세션 lazy cleanup/A-2 서버 검증 완료 (사용자 SSH 진행)**
   - A-1: `SADD user:sessions:2 fake-ghost-000000000000` 주입 → `GET /api/v1/admin/sessions/2` 호출 시
     `activeSessionCount:0, sessionIds:[]` 반환 + `SMEMBERS user:sessions:2` 빈 배열 → 유령 자동 SREM 확인
   - A-2: admin 로그인 세션(`user:sessions:5`, JWT에 `sessionId` claim 포함) 로그아웃 →
     해당 sessionId만 `user:sessions:5`에서 삭제(빈 배열), 다른 유저(`user:sessions:2`) 세션은 유지 → 기기별 격리 확인
   - 부수: logout이 없는 refreshToken을 받아도 `AuthServiceImpl.logout`의 `ifPresent` 멱등 처리로 SUCCESS
     (unknown token 무시) — 의도된 동작
   - 플랜 016 구현 계획 표에 A-1/A-3/B-1~3/D-1 완료 마킹 + 검증 결과 기록
4. **이력서 서버사이드 PDF 생성 (Phase 5 v2, 설계 023)** — OpenPDF 순수 Java
   - 폰트: Spoqa Han Sans v2.1.2 subset (SIL OFL 1.1) Regular/Bold TTF → `src/main/resources/fonts/` + LICENSE_OFL.txt
     (naver/nanumfont raw GitHub 404, Ubuntu archive 404, nanumfont 릴리즈는 GothicCoding뿐 → Spoqa 채택)
   - 의존성: `com.github.librepdf:openpdf:1.3.30` (build.gradle.kts)
   - `ResumePdfService`/`ResumePdfServiceImpl`: A4 단일 form, 헤더(이름 26pt Bold+연락처) +
     자기소개→경력→학력→스킬→자격증→프로젝트→포트폴리오(빈 섹션 생략), 2열 표(기간 우측 정렬 `yyyy.MM`, 종료 null→현재),
     static holder 폰트 로드(`createFont(path, IDENTITY_H, EMBEDDED, false, bytes, null)`), 프로필 null에도 유효 페이지
   - API: `GET /resume/api/v1/view/pdf` → `ResponseEntity<byte[]>` application/pdf,
     `Content-Disposition attachment; filename*=UTF-8''이력서.pdf` (RFC 5987)
   - 프론트: ResumeViewPage "PDF 다운로드" 버튼(기존 `apiDownload` 재사용) — print 버튼과 별개
   - 테스트(`ResumePdfServiceImplTest` 3건): %PDF+길이, 내용 추출(홍길동/경력/네이버/Spring Boot), 빈 뷰 유효, null 날짜 "현재" 렌더
     → 전체 resume 백엔드 테스트 통과(77건) + 프론트 `tsc -b`/`vite build` 성공
   - OpenPDF 상이점: byte[] 오버로드 없음 → (name, enc, embedded, cached, bytes, null) 시그니처,
     `PdfTextExtractor`는 인스턴스(`new PdfTextExtractor(reader)` + `getTextFromPage(int)`)
   - 커밋: `88b02d6`

## 변경된 파일 목록
- `modules/scraper/backend/.../repository/CompanyBlacklistRepository.java`: LEFT JOIN FETCH DISTINCT 쿼리 (N+1 방지)
- `modules/scraper/backend/.../service/CompanyBlacklistService.java`: `stats()` + `BlockStatsResponse` record 신규
- `modules/scraper/backend/.../controller/CompanyBlacklistController.java`: `GET /company-blacklist/stats` 추가
- `modules/scraper/backend/src/test/.../service/CompanyBlacklistServiceTest.java`: stats/normalizedNames 테스트 추가
- `platform/frontend/src/api/dashboard.ts`: `BlockStats`/`fetchBlockStats` 신규
- `platform/frontend/src/pages/Dashboard.tsx`: 차단 회사 유형/차단 사유 카드 + `CategoryBars` 컴포넌트 추가
- `docs/resume/ddl-resume-v7.sql`: 신규 — 학교/전공 마스터 시드 (고등학교 88·대학원 38·대학교 13·전공 30)
- `.github/workflows/deploy-backend.yml`: resume DDL v7 실행 라인 추가
- `docs/plans/022-260830-company-blacklist-category-design.md`: Phase 7 완료 표시
- `docs/plans/016-260825-redis-monitoring-roadmap-design.md`: A-1/A-3/B/D 완료 마킹 + 서버 검증 체크리스트
- `docs/plans/023-260831-resume-pdf-openpdf-design.md`: 신규 — 서버사이드 PDF 설계 (FR/구현 계획 ✅)
- `modules/resume/backend/build.gradle.kts`: OpenPDF 1.3.30 의존성 추가
- `modules/resume/backend/src/main/resources/fonts/`: SpoqaHanSans{Regular,Bold}.ttf + LICENSE_OFL.txt 신규
- `modules/resume/backend/.../domain/ResumePdfService.java`: 신규 — `generatePdf(userId)` 인터페이스
- `modules/resume/backend/.../domain/ResumePdfServiceImpl.java`: 신규 — OpenPDF 렌더 구현
- `modules/resume/backend/.../api/ResumeViewController.java`: `GET /view/pdf` 엔드포인트 추가
- `modules/resume/backend/src/test/.../domain/ResumePdfServiceImplTest.java`: 신규 — PDF 생성 테스트 3건
- `modules/resume/frontend/src/pages/ResumeViewPage.tsx`: "PDF 다운로드" 버튼 추가

## 이슈 및 해결
- `Comparator.comparingInt` 사용 시 `count(long)` 메서드 레퍼런스 불일치 → `comparingLong`으로 수정
- 통계 집계 키를 `reason.id`로 하니 테스트 `BlockReason.of()`가 id 미설정(null)이라 한 건으로 병합되는 문제
  → `name`(UNIQUE) 기준으로 키잉. 실데이터에도 동일 카테고리는 name이 같으므로 집계 결과 동일.

## 커밋
| 커밋 | 내용 |
|------|------|
| 6515184 | feat: 블랙리스트 카테고리별 차단 통계 API + 대시보드 차단 회사 유형/사유 |
| 056420b | feat: 학교/전공 마스터 확장 — ddl-resume-v7 + 배포 워크플로우 DDL 라인 |
| 5d3958c | docs: 플랜 016 완료 마킹 + AGENTS.md 배너 갱신 (Redis 검증 안내) |
| 5832bb2 | docs: Redis 서버 검증 결과 기록 (A-1 유령 cleanup / A-2 기기별 로그아웃) |
| 88b02d6 | feat: 이력서 서버사이드 PDF 생성 (OpenPDF) — /view/pdf API + PDF 다운로드 버튼 |

## 향후 작업
1. **resume Phase 3 잔여 도메인 / Phase 4+** (이전 핸드오프 `docs/daily/2026-08-21-todo.md` 복귀 지점 다시 확인)
   - 📌 2026-08-31 확인: Phase 3~7 + 벤치마킹 Phase 1~4는 이미 전부 구현 완료 (백엔드 77건 테스트 통과).