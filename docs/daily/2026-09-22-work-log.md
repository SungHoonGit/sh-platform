# 2026-09-22 작업 일지

## 작업 환경
- 날짜: 2026-09-22 (화)
- 시간: 세션 시작 시점 기준
- 브랜치: master (원격 동기화 완료, 신규 커밋 없음)

## 세션 시작 상태 (현 상황 파악)
- **마지막 작업**: 9/17 — 설계 031 회사 관리 페이지 피드백 6차까지 완료·배포 (마지막 커밋 `227333b`)
- **CI**: 최근 8건 전부 success, 이후 신규 배포 없음 (9/18 04:14 마지막 run)
- **서비스**: platform 200 / auth 200 / scraper 401(인증 게이트 정상) / resume actuator 200·swagger 200
- **SSH**: 이 기기에 `oci-web` alias·OCI 키 없음(`vm2_key`만 존재) → 서버 확인은 배포 워크플로우 SSH 진단 단계로 수행
- **9/17 인계 잔여**:
  1. (사용자) 실사용 검증 — /companies 전 기능 + viewer + 통합검색
  2. (cron) 9/18 03:30부터 자동 백업이 돌았는지 미확인 (9/17에 cron 패키지 설치·수동 검증까지 완료 상태)
  3. binlog 영속화·portfolio 제외는 9/17에 완료 처리됨

## 오늘 완료된 작업
1. [현 상황 점검]: git pull(동기화 완료)·CI·4개 서비스 헬스 확인 (platform/auth/resume 200, scraper 401=정상)
2. [배포 워크플로우 진단 보강]: SSH 진단 단계에 파일 백업 목록·cron 데몬 상태·cron journalctl·주기 백업 로그 tail 추가 → dispatch로 백업 자동 실행 여부 확인
3. [경량 SSH 진단 워크플로우 신설]: `.github/workflows/diag-ssh.yml` (빌드 없이 ~10초, `gh workflow run diag-ssh.yml`)
4. [백업 cron 미실행 근본 원인 확정·수정]:
   - **원인**: `infra/cron.d/sh-platform-mysql-backup` 마지막 줄 개행(\n) 누락 → Debian cron `Missing newline before EOF, this crontab file will be ignored`로 **파일 전체 무시**. 9/17 설치 이후 매 deploy RELOAD→ERROR 반복, 03:30/03:35 스케줄 전혀 발화 안 함 (9/18~9/21 백업 누락)
   - **수정**: 소스 파일 개행 추가 + 주의 주석, 배포 워크플로우에 멱등 개행 가드(`tail -c1` 검사) 추가
   - **검증**: 수동 실행 성공 — 20260922 3DB 덤프(sh_pass 24K/scraper 2.4M/resume 20K) + binlog-status.txt. 파일 백업도 수동 실행으로 오늘분 커버
   - 서버 TZ=Asia/Seoul 확인(CRON_TZ는 무해·권장), cron 프로세스 `/usr/sbin/cron -f -P` 가동 중
5. [오류 문서]: `docs/errors/015-260922-cron-d-missing-newline-error.md` + 가이드 009 §6 문제 해결 표 등재
6. [수정 배포·검증 완료]:
   - deploy run 35674143374 success → `cat -A` 두 스케줄 줄 모두 `2>&1$` (개행 복구 확인)
   - **cron 파싱 수락 확인**: `Sep 22 10:06:01 RELOAD` 이후 ERROR 없음 (과거 모든 RELOAD는 즉시 `Missing newline` ERROR 동반)
   - 수동 실행 재확인: MySQL 3DB 12M exit=0 + 파일 백업 `20260922/{data 604K, uploads 924K}` exit=0
   - 서버 TZ Asia/Seoul, cron 프로세스 `/usr/sbin/cron -f -P` 가동 중
7. [설계 031 피드백 7차 구현·배포] (`5ad5984`): 키워드 필드 의미 분기 + 편집 탭 정리
   - **BE**: `CompanyNoteRepository.findTagRowsByNoteIds` 배치 쿼리(N+1 회피) + `CompanyNoteService` 목록 응답에 메모 태그 포함 (차단 탭은 키워드 열에서 차단 키워드를 보여주므로 태그 미조회)
   - **목록 키워드 열**: 차단행=차단 키워드(red mono 정규화명), 메모행=태그 chips, 그 외 `-` (전 행 정규화명 표시 제거 — "회사명이 또 나오네" 피드백)
   - **슬라이드 보기 탭**: 상단 회사명 키워드 라인 제거, 차단 섹션은 `차단 키워드: xxx` + 차단 카테고리 chips(붉은 섹션), 메모 태그 chips는 비차단만
   - **슬라이드 편집 탭**: 붉은 박스 `해제` 버튼·`차단 편집` 버튼 제거(헤더 Ban 아이콘·인라인 카테고리 TagInput과 중복 → 차단 키워드는 read-only 표기, 카테고리 인라인 즉시저장 유지), 메모 태그 필드는 비차단 행에서만 노출, 차단 행 저장 시 태그 필드 미전송(기존 노트 태그 보존)
   - **로컬 검증**: `:modules:scraper:backend:test` 전체 통과(+목록 태그 신규 테스트), FE `tsc -b && vite build` 통과, `oxlint` 무경고. 기능 확인은 배포 후 운영에서 (로컬 서버 환경 미사용 프로세스)
   - 비고: 차단 키워드 직접 편집 UI는 제거 — 헤더 Ban(해제→재차단)으로 갱신 가능, rename 시 노트 정규화명 연동 서버 로직은 유지
8. [설계 031 피드백 7차b — 차단행 키워드 = 차단 카테고리]:
   - **피드백**: 차단 행 키워드 열에 정규화명(회사명과 거의 동일)이 남아 "수정 안 됨" — 원하는 것은 `스타트업` 같은 **차단 카테고리**
   - **BE**: `CompanyNoteResponse.blockReasons` 필드 추가. blacklist 조회는 기존 `LEFT JOIN FETCH blockReasons` 재사용(N+1 없음). 차단탭/전체탭 차단행 모두 채움
   - **FE 키워드 열**: 차단행=차단 카테고리 chips(빨강), 메모행=태그 chips(회색), 그 외 `-` — 정규화명 표시는列表에서 완전 제거(슬라이드 보기·편집의 '차단 키워드:' 라인은 유지)
   - 검증: 백엔드 전체 테스트(+차단 카테고리 assert), FE build·lint 통과 → 배포 후 운영 확인
9. [resume 프로젝트 섹션 개편 — 기술스택 chips·기간 달력범위·설명 markdown·목록 미리보기] (`c6f7ab5`):
   - **신규 컴포넌트**: `TagInput.tsx`(서버 suggest 검색 → 칩 변환, comma↔chips), `DateRangePicker.tsx`(달력 범위선택, start≤end 검증), `MarkdownText.tsx`(react-markdown 10, raw HTML 비활성 XSS 방지)
   - **CrudSection**: `FieldDef`에 `dateRange`/`markdown`/`maxLength` 추가, skill 타입 → TagInput+`/reference/skills/search`, markdown → textarea+미리보기, `renderRowDetail` prop(행 클릭 토글 상세, 버튼 stopPropagation), subtitle에서 기간 `start ~ end|진행 중` 표시
   - **EditPage projects**: startDate→dateRange, techStack maxLength 300(DB @Size 일치), description→markdown, renderRowDetail=기술스택 chips+마크다운 미리보기. portfolioConfig 동기화
   - **템플릿 3종+PortfolioCard**: description `<p className="whitespace-pre-wrap">` → `<MarkdownText>`
   - **CSS**: `.md-view` 스타일 신규 (index.css)
   - **검증**: FE `npm run build`(tsc -b && vite build) 통과 → `c6f7ab5` push → deploy run 35709805432 success → 실서버 `/resume/` 번들(`index-BR4QGt1O.js`)에 `md-view`·`진행 중` 포함 확인. BE/DDL 변경 없음
10. [설계 032 — 사람인/잡코리아 경력 필터 compound 매핑 DB화]:
   - **배경**: 9/15 재진단에서 기록한 후속 과제 — `exp_cd`+`exp_min/max`(사람인), `careerList`+`careerMin/Max`(잡코리아)는 단일 코드 문자열 `value_mapping`으로 표현 불가라 switch 유지 → 복합 value type 추가로 해소
   - **DDL v13**: `value_type` ENUM에 `compound` 추가 + 사람인/잡코리아 career 행을 compound 시드로 멱등 마이그레이션. deploy 워크플로우에 라인 추가
   - **BE**: `ValueType.compound` enum, `SiteSearchMapper.mapCompoundParams`(표준값→파라미터 Map, 실패 시 빈 Map=fallback 신호), `toSiteParams`는 compound 스킵, 관리 API `validateMapping`에 compound 중첩 객체 검증
   - **크롤러**: `SaraminCrawler.resolveCareerParams`·`JobkoreaCrawler.resolveCareerParams` — DB compound 우선 → 없으면 기존 switch fallback (무중단)
   - **관리 UI**: `master.ts` ValueType·AdminMaster 셀렉트에 compound 노출 + placeholder 예시
   - **테스트**: SiteSearchMapperTest compound 8건, SiteSearchMappingServiceTest compound 2건, CrawlerMappingTest resolveCareerParams 8건 — 전체 scraper 테스트 통과, platform FE tsc+vite build+oxlint 통과
   - **검증**: `./gradlew :modules:scraper:backend:test` BUILD SUCCESSFUL → `c630c39` push → deploy run 35818763810 success → 실서버 `/scraper/v3/api-docs`에 `valueType enum: direct,mapped,range,compound` 확인 (DDL v13 기동 전 적용 + 새 enum 서빙)

## 변경된 파일 목록
- `.github/workflows/deploy-backend.yml`: 진단 단계 보강 + cron.d 개행 가드
- `.github/workflows/diag-ssh.yml`: 신규 (SSH 전용 경량 진단)
- `infra/cron.d/sh-platform-mysql-backup`: 마지막 개행 추가 + 주의 주석
- `docs/errors/015-260922-cron-d-missing-newline-error.md`: 신규 오류 기록
- `docs/guides/009-260914-mysql-backup-guide.md`: §6 문제 해결 表 1행 추가
- `modules/scraper/backend/.../repository/CompanyNoteRepository.java`: 배치 태그 쿼리 추가
- `modules/scraper/backend/.../service/CompanyNoteService.java`: 목록 응답에 메모 태그 배치 포함
- `modules/scraper/backend/.../test/.../CompanyNoteServiceTest.java`: 목록 메모 태그 테스트 +1
- `modules/scraper/frontend/src/api/companies.ts`: `CompanyNoteItem.categories` 추가
- `modules/scraper/frontend/src/pages/Companies.tsx`: 키워드 열 분기(차단/태그 chips/-)
- `modules/scraper/frontend/src/components/CompanySlideOver.tsx`: 보기 키워드 라인 제거·차단 섹션 정리·편집 탭 중복 제거·태그 필드 분기
- `modules/resume/frontend/src/components/{TagInput,DateRangePicker,MarkdownText}.tsx`: 신규
- `modules/resume/frontend/src/components/CrudSection.tsx`: FieldDef dateRange/markdown/maxLength + renderRowDetail + skill TagInput
- `modules/resume/frontend/src/pages/EditPage.tsx`: projects 섹션 기간·마크다운·미리보기
- `modules/resume/frontend/src/config/portfolioConfig.ts`: startDate→dateRange, description→markdown, techStack maxLength
- `modules/resume/frontend/src/components/templates/{Classic,Modern,Saramin}Template.tsx`, `shared.tsx`: description → MarkdownText
- `modules/resume/frontend/src/index.css`: `.md-view` 스타일
- `modules/resume/frontend/package.json`: react-markdown ^10.1.0
- `docs/plans/032-260922-career-compound-mapping-design.md`: 신규 설계
- `docs/scraper/ddl-v13.sql`: compound ENUM + 사람인/잡코리아 career 시드
- `.github/workflows/deploy-backend.yml`: ddl-v13 라인 추가
- `modules/scraper/backend/.../SiteSearchMapping.java`: ValueType.compound
- `modules/scraper/backend/.../SiteSearchMapper.java`: mapCompoundParams + convertValue compound 스킵
- `modules/scraper/backend/.../SiteSearchMappingService.java`: compound 중첩 객체 검증
- `modules/scraper/backend/.../SaraminCrawler.java`, `JobkoreaCrawler.java`: resolveCareerParams DB 우선
- `modules/scraper/backend/.../test/...`: SiteSearchMapperTest·SiteSearchMappingServiceTest·CrawlerMappingTest compound 테스트
- `platform/frontend/src/api/master.ts`, `pages/admin/AdminMaster.tsx`: compound 타입·셀렉트

## 이슈 및 해결
- [주기 백업 5일치 누락(9/18~9/21)]: cron.d 개행 누락으로 파일 전체 무시 → 수정·가드 적용. 과거 시점 스냅샷은 복구 불가, binlog(9/17~)로 변경분 이론적 복구 가능. 오늘(9/22)분은 수동 실행으로 확보.

## 커밋 내역
| 시간 | 커밋 | 내용 |
|------|------|------|
| | 14836f6 | chore(ci): 백업 cron 자동 실행 확인용 진단 단계 보강 + 신규 일지 |
| | c2face5 | chore(ci): SSH 전용 경량 진단 워크플로우 추가 (diag-ssh.yml) |
| | af73f6f | fix(infra): cron.d 마지막 개행 누락으로 백업 스케줄 전체 무시되던 문제 수정 |
| | 305461b | docs: 9/22 일지·AGENTS 갱신 (백업 수정·diag 워크플로우) |
| | 5ad5984 | feat(scraper): 031 피드백 7차 — 키워드 열 분기·편집 탭 정리 |
| | 84e73bc | feat(scraper): 031 피드백 7차b — 차단행 키워드 열에 차단 카테고리 표시 |
| | 8c10309 | docs: 9/22 일지 — 피드백 7차b(차단 카테고리 키워드) |
| | c6f7ab5 | feat(resume): 프로젝트 섹션 — 기술스택 chips·기간 달력범위·설명 markdown·목록 미리보기 |
| | c630c39 | feat(scraper): 사람인/잡코리아 경력 필터 compound 매핑 DB화 (032) |
| | d9a45e7 | docs: 9/22 일지·AGENTS — 설계 032 경력 필터 compound DB화 기록 |

## 향후 작업
1. **내일(9/23) 03:30/03:35 KST 첫 자동 발화 확인** — `gh workflow run diag-ssh.yml` 후 BACKUP DIRS/LOG TAILS에서 20260923 확인
2. (사용자) 실사용 검증(배포 후 운영 확인 프로세스):
   - **피드백 7차**: /companies 키워드 열(차단=차단 키워드 red, 메모=태그 chips, 그 외 -), 슬라이드 보기(상단 키워드 라인 제거·차단 섹션 '차단 키워드'+카테고리 chips), 편집(해제/차단편집 버튼 제거·메모 태그는 비차단만·차단 행 저장 시 태그 보존)
   - 기존: viewer + 통합검색
   - **resume 프로젝트 개편**: 편집 탭 프로젝트 행 기술스택→chips 입력(suggest 검색), 기간→달력 범위, 설명→마크다운+미리보기, 행 클릭 시 목록 하단 상세 미리보기(기술스택 chips+마크다운 렌더), 템플릿 description 마크다운 렌더 확인
   - **설계 032**: /platform/admin/master에서 사람인·잡코리아 career 행이 compound 타입·중첩 JSON으로 보이는지, 통합검색/크롤링에서 경력 필터 동작 유지·변경 확인
