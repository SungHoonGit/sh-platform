# 2026-08-04 작업 일지

## 작업 환경
- 날짜: 2026-08-04 (화)
- 시간: 17:45 기준
- 브랜치: master

---

## 오늘 완료된 작업

### 1. 엑셀 내보내기 기능 (FastExcel)
- **FastExcel 1.2.0** 의존성 추가
- **JobPostingVO** 엑셀용 DTO 클래스 생성
- **JobPostingController** `/job-postings/export` 엔드포인트 추가
- Viewer: 서버 기반 DB → XLSX 내보내기
- Search: 클라이언트 기반 xlsx 라이브러리로 XLSX 내보내기
- **시트별 분리**: 전체 + 사이트별 (사람인, 잡코리아, 원티드, 리멤버)

### 2. 뷰어 UI 개선
- 폰트 크기 축소 (text-[12px]/[13px])
- 스케줄 이름 + 날짜 표시
- 엑셀 다운로드 버튼 추가

### 3. 공고 수집 알림 개선
- "크롤링 중" → "공고 수집 중" 문구 변경
- **헤더 알림 버튼** 추가 (Bell 아이콘 + 드롭다운)
- **disconnected 상태** 추가 (연결 끊김 시 관대한 처리)
- 알림 지속성: 글로벌 Context로 모든 페이지에서 확인 가능
- **Bell 아이콘 항상 표시** (progressList와 무관)
- **고정된 Toast 위치 제거** (Layout.tsx)
- **중복 알림 방지** (같은 configId)
- **EventSource 연결 실패 시** '서버 연결 실패' 표시

### 4. 스케줄 관리 UI 개선
- 수정 시 별도 영역에서 열리도록
- 사이트 구분자 `|-|` 제거
- 경로 필드 제거
- 아이콘 커스터마이징

### 5. DDL 통합 정리
- **ddl-v2.sql**로 모든 테이블 통합
- 삭제: ddl.sql (구버전), ddl-account-isolation.sql, ddl-job-postings.sql, ddl-site-search-mapping.sql
- 삭제: update-site-search-mapping-jobkorea.sql (일회성 스크립트)
- crawl_config에 `schedule_icon` 컬럼 추가
- crawl_data 테이블 유지 (deprecated, 메타데이터용)

### 6. 문서 작업
- Java Excel 라이브러리 비교 분석 문서
- FastExcel 상세 분석 문서
- 2026-08-04 작업 일지 (이 파일)

---

## 변경된 파일 목록

### 백엔드
- `modules/scraper/backend/build.gradle.kts` - FastExcel 의존성
- `modules/scraper/backend/.../api/dto/JobPostingVO.java` - 엑셀 DTO
- `modules/scraper/backend/.../repository/JobPostingRepository.java` - 쿼리 메서드
- `modules/scraper/backend/.../controller/JobPostingController.java` - 엑셀 내보내기 API

### 프론트엔드
- `modules/scraper/frontend/package.json` - xlsx 의존성
- `modules/scraper/frontend/src/pages/Viewer.tsx` - UI 개선 + 엑셀 다운로드
- `modules/scraper/frontend/src/pages/Search.tsx` - XLSX 내보내기
- `modules/scraper/frontend/src/components/CommonHeader.tsx` - 헤더 알림
- `modules/scraper/frontend/src/contexts/CrawlProgressContext.tsx` - disconnected 상태
- `modules/scraper/frontend/src/components/CrawlProgressToast.tsx` - disconnected 표시

### 문서
- `docs/guides/java-excel-library-comparison.md`
- `docs/guides/fastexcel-analysis.md`

---

## 이슈 및 해결

### "수집 오류" 표시 문제
- **원인**: 페이지 이동 시 EventSource 연결 끊김 → onError 트리거
- **해결**: disconnected 상태 추가 (에러보다 관대한 표시)
- **서버 동작**: 크롤링은 서버에서 독립 실행, 데이터는 정상 저장

### 헤더 알림 버튼 미개발
- **해결**: Bell 아이콘 + 드롭다운으로 수집 현황 표시

---

## 커밋 내역

| 시간 | 커밋 | 내용 |
|------|------|------|
| 07:10 | `f9ef238` | 엑셀 내보내기 개선 (시트별 분리 + XLSX + 폰트) |
| 07:27 | `5859d98` | 공고 수집 알림 개선 (텍스트 + 헤더 알림 + disconnected) |
| 17:30 | `ce0665c` | DDL 통합 정리 (ddl-v2.sql로 모든 테이블 통합) |
| 17:45 | `19318b7` | 알림 시스템 개선 (Bell 항상표시 + 중복방지 + 에러표시) |

---

## 향후 작업

1. 스케줄링 로그 관리자 페이지 (테넌트 ID + 관리자 권한)
2. 기존 daily 로그 파일 정리 (위치 이동)
