# 2026-09-16 작업 일지

## 작업 환경
- 날짜: 2026-09-16 (화)
- 시간: 14:14 KST 기준
- 브랜치: master

## 오늘 완료된 작업
1. [백업 2회차 확인]: 오늘 03:30/03:35 cron 산출물 없음 확인 → 문제로 기록 (아래 이슈 참조)
2. [설계 031]: 회사 관리 페이지(별점+MD+북마크/차단+슬라이드) 설계 문서 작성
3. [031 Phase 1]: DDL v10 + company-notes 백엔드 CRUD + 테스트 12건
4. [031 Phase 2]: /companies 프론트 (목록+슬라이드오버) — react-markdown, SpaFallback 라우트
5. [031 Phase 3]: viewer 회사명 클릭 → 슬라이드 연동 (기존 메모 조회 후 상세/신규 분기, normCompany 매칭)
6. [031 피드백 반영]: 전체 탭에 차단 표시·차단 키워드+숨김수·차단/북마크 상호배타·slate 테마 통일·추가 모달
7. [031 피드백 2차]: 별점↔북마크 불변식(별=북마크 필수)·차단 키워드 편집(등록과 동일 다이얼로그)·슬라이드 등장 애니메이션·삭제 버튼 스타일·그리드 정렬(회사명/별점/업데이트)
   - 참고: CompanyNoteService/Test 2건이 PowerShell 재작성으로 LF→CRLF 바뀌어 diff가 크게 잡힘(내용 동일, 테스트 통과). 기존 자바 파일도 CRLF라 유지.
- [피드백 2차 배포 확인]: 새 번들(index-B5yUQzEZ.js) 서빙, 백엔드 401(정상 기동).

## 변경된 파일 목록
- `docs/plans/031-260916-company-manage-design.md`: 신규 설계 (company_notes 테이블, /company-notes API, /companies UI)
- `docs/daily/2026-09-16-work-log.md`: 신규 일지
- `docs/scraper/ddl-v10.sql` + `ddl-v11.sql`: company_notes 테이블 신규·my_stars INT 수정 (멱등)
- `.github/workflows/deploy-backend.yml`: ddl-v10/v11 실행 라인 추가
- `modules/scraper/backend/...`: CompanyNote Entity/Repository/Service/Controller + DTO 3종 + Service 테스트 12건, CompanyBlacklistRepository 조회 1건 추가, SpaFallback `/companies`
- `modules/scraper/frontend/...`: react-markdown 설치, `api/companies.ts`, `pages/Companies.tsx`, `components/CompanySlideOver.tsx`, `components/Stars.tsx`, App 라우트·Layout 내비, `.md-view` 스타일

## 이슈 및 해결
- [백업 2회차 미실행]: 15:54(KST) 배포 로그 BACKUP DIR에 `20260915/` 없음 → 9/15 03:30 cron 미동작. 일자 디렉토리가 전혀 없으므로 cron 자체가 실행 안 됨. 추가로 9/14 초회분에 portfolio_platform 덤프 누락(3개만 존재). → SSH 확인 필요: cron 서비스 상태, /etc/cron.d 파일, portfolio DB 존재 여부. 다음 배포 로그에서 재확인 예정.
- [031 Phase 1 배포 후 scraper 502 지속]: CI 35059597845 success, DDL v10 적용 확인(set -e 통과), 재시작 후 12분 경과해도 `/scraper/*` 502. nginx 정상(SPA 200), auth/resume 200. 배포 로그에 신규 프로세스 기동 크래시(ERROR) 없음 → 저속 cold start 또는 포트 충돌 재시작 루프(AGENTS 기재 기존 이슈) 의심. 로컬 SSH 불가 → 사용자 서버 확인 요청.
- [원인 확정·수정]: `Schema-validation: wrong column type [my_stars] found [tinyint], expecting [integer]` — DDL v10의 TINYINT vs 엔티티 Integer 불일치(내 실수). Boolean↔TINYINT(1)은 Region 선례대로 통과 패턴이라 is_bookmarked는 유지. DDL v11(MODIFY→INT)로 수정 후 재배포.
- [복구 확인]: DDL v11 배포 후 scraper 정상 기동, api-docs에 `/company-notes`(GET/POST), `/company-notes/{id}/export`, CompanyNote 스키마 등록 확인.
- [Phase 2 배포 확인]: `/platform/scraper/companies` 200 + 새 번들(index-C1Dr19lI.js, 로컬 빌드 동일 해시), api-docs 200·/company-notes 등록. tsc+vite·oxlint·백엔드 테스트 전부 통과.
- [Phase 3 배포 확인]: `/platform/scraper/viewer` 200 + 새 번들(index-BhMbf2jh.js), `/scraper/sites` 401(정상 기동). viewer 회사명 클릭 → 메모 조회 후 상세/신규 슬라이드.
- [피드백 반영 배포 확인]: 새 번들(index-C6xAHFlI.js) 서빙, 백엔드 401(정상 기동). 전체 탭 차단 표시·키워드+숨김수·상호배타·slate 통일·추가 모달.
- [후속 수정]: `/scraper/companies` 직접 접근 401 → SpaFallback 라우트는 추가했으나 SecurityConfig permitAll에 `/companies` 누락이 원인. 1줄 수정 후 재배포, 200(index.html) 확인. `/company-notes` API는 401 유지(인증 경계 정상).

## 커밋 내역
| 시간 | 커밋 | 내용 |
|------|------|------|
| 14:xx | (다음 커밋) | docs: 설계 031 회사 관리 페이지 + 9/16 일지 |
| (다음 커밋) | feat(scraper): 031 Phase 1 — company-notes CRUD API + DDL v10 |

## 향후 작업
1. 설계 031 Phase 1 구현 (DDL v10 + 백엔드 CRUD + 테스트)
2. (사용자) 실사용 검증 — 통합검색 saramin/jobkorea, 뷰어 수집 확인
3. (사용자 수동) cron 미동작 + portfolio 덤프 누락 SSH 확인
4. (사용자 수동) DB 서버 `99-binlog.cnf` binlog 14일 영속화
