# 2026-08-26 작업 일지

## 작업 환경
- 날짜: 2026-08-26 (수)
- 시간: 새벽~오전 세션
- 브랜치: master

## 완료된 작업

### 1. Redis 운영화 마무리 (플랜 016)
- A-1 유령 세션 lazy cleanup (`09e66ea`): `SessionService.pruneAndGetActiveSessions()` — 조회 시 Hash 만료 멤버 SREM. 재판정: 관찰된 유령은 수동 청소 잔여였음(오류 아님), TTL 비대칭 저빌도 누적만 hardening 대상
- redis-exporter v1.67.0 서버 설치 + systemd 등록 → Prometheus scrape 연결 (`redis_up 1`)
- Grafana 대시보드 임포트(11835) + 알림 규칙 4건 (down/메모리/거부/축출) — 사용자 적용
- 부수: 생성물 개행 정규화 CRLF→LF + `.gitattributes` (`9da7d84`)

### 2. 공고탐색 스코핑 (`b261030`)
- 증상: 스케줄 미등록 계정에서도 타인 수집 공고 전체 조회됨
- 원인: `/job-postings/recent`가 설계상 전체 브라우징(공용 잡보드형)
- 수정: JPQL에 `j.config.accountId = :accountId` 필터 — "본인이 스케줄로 긁어온 것만" 정책 확정
- 파생 정책: 설정 없는 계정은 빈 화면이 정상

### 3. 감사 로그 이중 기록 C-audit (`5488f2f`)
- 신규 테이블(sh_pass): `login_logs`, `admin_audit_logs` — DDL 자동 실행 확인
- Redis=실시간 판정(hot) / MariaDB=증적·리포트(cold) 이중 구조
- 감사 포인트: ROLE_CHANGE(before→after 역할), DELETE_USER(email 스냅샷), FORCE_LOGOUT
- actor는 컨트롤러에서 `SecurityUtils.currentAccountId()`로 받아 서비스에 전달(도메인 보호)
- 감사 실패는 비즈니스 흐름 차단 안 함(로그만)
- 버그 예방: AdminService 클래스 readOnly 위 쓰기 메서드에 @Transactional 명시 보강

### 4. 기기별 로그아웃 A-2 (`ba93930`)
- 기존: 한 기기 로그아웃 = `removeAllSessions`로 전 기기 세션 삭제 (멀티디바이스 정책과 모순)
- 구현: accessToken claims에 `sessionId` 심기 → 로그아웃 시 해당 세션만 삭제(레거시 토큰은 전체 삭제 폴백)
- refresh 시 sessionId 보존: Redis 리프레시 값 형식 확장 `userId` → `userId:sessionId` (getSessionId 파싱, 레거시 호환)
- OAuth2SuccessHandler: 세션 생성→토큰 발급→리프레시 저장 순서로 sessionId 일관 주입

### 5. Redis 영속성·메모리 설정 D-1/D-2 (서버)
- AOF 활성화: 런타임 CONFIG SET + redis.conf 영구반영(`appendonly yes`, 1380행) — `aof_enabled:1, status:ok`
- 멀티파트 AOF 생성 확인(base.rdb + incr.aof + manifest, /var/lib/redis/appendonlydir/)
- maxmemory 256mb + volatile-lru 적용 (TTL 키 우선 축출 — 세션 데이터 특성 고려)
- 가이드 009에 §7.5 운영설정 섹션 추가

### 6. 감사 로그 조회 API (`b5cd72a`) + 낡은 핸드오프 정정
- `GET /api/v1/admin/audit?action=&actorUserId=&targetUserId=&page=&size=` — 감사 로그 최신순 페이징 조회
- 발견: 구 핸드오프(08-21)의 "resume Phase 3 진행중"은 낡은 정보 — 실제로는 8/24 세션에서 6개 도메인 전부 구현·테스트 완료(12종 서비스 테스트 존재, 빌드 그린) 확인
- 교훈: 핸드오프 문서는 "현재 상태"가 아니라 "검증된 상태"를 적을 것

## 커밋 내역
| 커밋 | 내용 |
|------|------|
| `09e66ea` | feat: 유령 세션 lazy cleanup |
| `9da7d84` | feat: prometheus redis 타겟 + 개행 정규화 |
| `b261030` | fix: 공고탐색 본인 수집분 스코핑 |
| `5488f2f` | feat: 감사 로그 이중 기록 (C-audit) |

## 검증 방법 (서버)
```bash
-- 감사 테이블 동작 확인
mysql -h 10.0.0.39 -u sh_user -p'SHpass1234!' sh_pass -e "SHOW TABLES LIKE '%audit%'; SELECT COUNT(*) FROM login_logs;"
-- 관리자 화면에서 강제로그아웃 1회 후:
SELECT * FROM admin_audit_logs ORDER BY id DESC LIMIT 3;
```

## 향후 작업
1. A-2 sessionId claim 기기별 로그아웃 (플랜 016)
2. D-1 AOF 영속성 (`appendonly yes`) — 서버 작업
3. 관리자 페이지에 audit 로그 조회 UI (선택)
4. resume Phase 3 복귀 (구 핸드오프 참조)

### 7. [장애] 미사용 파생쿼리로 auth 크래시 루프 → 수정 + JPA 스모크 방역 (`7474556`)
- 증상: 배포 직후 Swagger 502, auth 재시작 반복
- 원인: C-audit에서 예비로 만든 `findByUserIdOrderByCreatedAtDesc(userId, since)` — 파라미터 2개 vs 조건 1개 불일치 → 기동 중 쿼리 생성 실패
- 본질: 이번 주 3번째 "CI 초록불 + prod 다운" (008 빈충돌 / 009 필터예외 / 010 JPA 쿼리)
- 해결: 해당 메서드 삭제 + `RepositoriesSmokeTest`(@DataJpaTest+H2) 추가 — 역방향 검증(고의 파손→FAILED 확인)까지 수행
- 상세: `docs/errors/010-260826-derived-query-startup-failure-error.md`

### 8. 관리자 UI — 감사 로그·세션 관리·애널리틱스 (`73ed5c5`)
- 신규 페이지: `AdminAudit.tsx`(행위/관리자ID 필터+페이지네이션 테이블, before→after 표시), `AdminSessions.tsx`(userId 조회→활성 세션 목록→강제로그아웃)
- AdminDashboard에 "오늘 로그인 현황"(성공/실패) 카드 추가 (`GET /admin/analytics`)
- 라우트: /platform/admin/audit, /platform/admin/sessions + 사이드바 관리 섹션 링크
- 로컬 풀빌드 검증 후 푸시 (Vite 8 rolldown macOS 네이티브 바인딩 별도 설치 필요했음)

### 9. 권한 관리 구체화 (`d58bc42`)
- 보호 규칙(백엔드 강제): 자기 계정 권한 변경 금지 / 관리자 페이지 자기 삭제 금지 / 마지막 ADMIN 권한 회수 금지(countByRole)
- 신규 페이지: AdminRoles(역할 정책 카드 + 보호 규칙 안내 + ROLE_CHANGE 이력 뷰), 라우트/네비 추가
- AdminUsers mutation에 403 에러 피드백 추가
- 테스트: AdminServiceTest 6건(성공/자기차단/마지막ADMIN차단/복수ADMIN허용/자기삭제차단/NOT_FOUND)
- 보안 와이어링 검증: @EnableMethodSecurity + JWT ROLE_ 접두사 매핑 확인

### 10. 실시간 검색결과 스크랩 지원 (`bfcafd6`)
- 배경: 검색 결과는 DB 미저장이라 ID 없음 → ☆ 불가 (8/24부터 남은 구조적 빈틈)
- 설계: 일괄 저장 대신 **☆ 클릭 시점에 단건 upsert 후 스크랩** (저장=좋아요 순간)
- DDL v5: config_id NULL 허용 + saved_by_account_id 컬럼 (검색 저장분은 크롤러 설정 없음)
- API: POST /scraper/api/v1/job-scrap/live — dedupKey(오늘자) 재사용, 멱등
- 공고탐색 스코핑 확장: 내 크롤러 수집분 OR 내가 저장한 검색분
- 프론트: Search.tsx 결과행 ☆ 버튼 (클릭 시 저장→★ 전환)
