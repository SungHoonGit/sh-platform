# 2026-08-01 작업 로그

## 1. 502 에러 조사 — 배포 중 일시 현상으로 판명 (수정 없음)

- 증상: 사용자가 `/scraper/` 접근 시 502 확인
- 확인 결과 (외부 curl + 헤드리스 브라우저):
  - `/scraper/` 200, `/scraper/docs/crawlers` 200, `/api/health` 200, `/platform/` 200
  - JS/CSS 에셋(`/scraper/assets/*`) 200, `index.html` 파일명 = 최신 빌드(AuthGuard 포함)와 일치
- 원인: 배포 직후 systemd 재시작 순간의 일시적 502였던 것으로 판단. 현재 정상.

## 2. 대시보드 → 스크래퍼 흰 화면 조사 및 수정 — 커밋 72fde97

### 조사 (puppeteer-core 헤드리스 재현)
- `/platform/` (비로그인) → **흰 화면 재현**: `pageerror: <Navigate> may be used only in the context of a <Router> component.`
  - 원인: `platform/frontend/src/App.tsx` — `localStorage` 토큰 없을 때 `<Navigate>`가 `BrowserRouter` **밖**에서 렌더링됨 (React Router v7 에서 크래시)
- `/scraper/` (비로그인) → 로그인 페이지로 정상 리다이렉트 (`/?redirect=/scraper/`)
- `/scraper/` (유효하지 않은 토큰, window.open으로 새 탭) → 로그인 페이지로 정상 리다이렉트
- `/scraper/` (유효 토큰, `/api/v1/auth/me` 응답 가로채기 mock) → **앱 정상 렌더링** (헤더 사용자명·로그아웃, 통합검색/스케줄등록/뷰어, 검색 폼)

### 수정
| 파일 | 변경 |
|------|------|
| `platform/frontend/src/App.tsx` | 비로그인 시 `<Navigate to="/">` (Router 밖 크래시) → `RedirectTo` 컴포넌트(`window.location.replace("/")`)로 대체 |
| `modules/scraper/frontend/src/components/AuthGuard.tsx` | 렌더 중 `window.location.replace` 직접 호출 → `useEffect`로 이동 (React 19 동시성 렌더링에서의 렌더 중 네비게이션 사이드이펙트 제거) |

### 검증
- platform / scraper 프론트 빌드 성공
- push `72fde97` → GitHub Actions 배포 자동 진행

### 확인 필요
- 배포 완료 후 사용자 재확인: 흰 화면이 계속 보이면 브라우저 콘솔(F12) 에러 또는 테스트 계정 필요
- 서버는 `Cache-Control: no-store` 반환 → 브라우저 캐시 원인 가능성 낮음

## 참고: 조사에 사용한 헤드리스 스크립트 (임시)
- `%TEMP%\opencode\scraper-check.mjs` — 페이지 렌더 상태/콘솔 에러 수집
- `%TEMP%\opencode\scraper-token.mjs` — 유효하지 않은 토큰 + window.open 시나리오
- `%TEMP%\opencode\scraper-auth.mjs` — `/api/v1/auth/me` 가로채기로 유효 로그인 상태 mock
- Edge 실행파일 `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe` + puppeteer-core

## 3. 에러 페이지 전략 + OWASP 보안 강화 — 설계 문서 승인 및 대표 구현 (미커밋)

### 설계 문서 (sh-platform 저장소 `docs/security/`)
- `error-pages.md` — 에러 처리 계층 분담(nginx=인프라 에러, SPA=라우팅 404/런타임 에러, 백엔드=API 404/405/400/500 JSON), 구현 체크리스트 11개, 검증 시나리오
- `owasp-hardening.md` — 버전 노출(server_tokens/Whitelabel/Whitelabel), 보안 헤더, scraper 정보노출(show_sql/DEBUG/health), nginx 설정 저장소 관리
- `docs/README.md` 보안 섹션, `docs/roadmap.md` 체크리스트 추가

### 플랫폼 프론트 (빌드 성공, 새 해시 `index-WMg6mlry.js`)
| 파일 | 내용 |
|------|------|
| `platform/frontend/src/pages/NotFound.tsx` (신규) | 404 화면 + "대시보드로 돌아가기" Link `/platform` |
| `platform/frontend/src/components/ErrorBoundary.tsx` (신규) | 런타임 에러 화면 + "새로고침" 버튼, `componentDidCatch`에서 console.error |
| `platform/frontend/src/App.tsx` | `<Route path="*" element={<NotFound />} />` catch-all 추가 |
| `platform/frontend/src/main.tsx` | `<ErrorBoundary>`로 `<App />` 래핑 |

### 공통 백엔드 (common)
- `GlobalExceptionHandler` 확장:
  - `NoResourceFoundException`/`NoHandlerFoundException` → 404 `NOT_FOUND`
  - `HttpRequestMethodNotSupportedException` → 405 `METHOD_NOT_ALLOWED`
  - `MethodArgumentTypeMismatchException`/`HttpMessageNotReadableException` → 400 `INVALID_INPUT`
  - `resolveMessage` 헬퍼: `NoSuchMessageException` 시 코드 그대로 반환 (메시지 누락 방어)
  - `errorResponse(status, code)` 헬퍼로 응답 통일
- `common/build.gradle.kts`에 `testImplementation("org.springframework.boot:spring-boot-starter-test")` 추가
- `GlobalExceptionHandlerTest` 신규 (Mockito, 404/405/400/500/fallback 8개 케이스) → `:common:test` 통과
- auth `messages_ko.properties`/`messages_en.properties`에 `METHOD_NOT_ALLOWED` 키 추가

### 설정 (OWASP 정보노출 정리)
- 4개 모듈 `application.yml`에 `server.error.whitelabel.enabled: false` 추가
- scraper/resume/portfolio: `show-sql: true→false`, `format_sql: true→false`, `health.show-details: always→when-authorized`, 모듈 로그 `DEBUG→INFO`

### nginx (저장소 버전 관리 시작, `infra/nginx/`)
- `502.html` — 브랜딩 서비스 점검 페이지 (경로: `/var/www/sh-platform/502.html`)
- `sh-platform.conf` — 참조용 설정(server_tokens off, 보안 헤더, error_page 502/503/504, SPA try_files, gzip)
- **적용 안내 필요**: 서버 `/etc/nginx/conf.d/` 실제 설정과 동기화는 SSH 접속 필요 → 사용자 안내

### 검증
- `:common:test` 통과 (GlobalExceptionHandlerTest 8건)
- `:common:build` + 4개 백엔드 `compileJava` 성공

### 커밋 예정 (아직 미커밋, 사용자 확인 대기)
- platform 프론트 + common 백엔드/테스트 + 설정 + nginx 샘플 + docs(security 2건, README, roadmap)
- **커밋 제외**: `.classpath/.project/.settings/bin` (IDE 생성물), `application-local.yml` (기존 로컬 변경)

## 4. 프론트 404/ErrorBoundary 확대 (scraper·auth) + nginx 보안 강화 적용 — 커밋 ccee36a, db076c1, 215b297

### 프론트 404/ErrorBoundary 확대 (커밋 `ccee36a`)
- auth: `NotFound`(다크 테마) + `*` catch-all, `ErrorBoundary` 래핑
- scraper: `NotFound` + `*` catch-all, `ErrorBoundary` 래핑
- 두 프론트 빌드 성공, 배포 워크플로가 서버에서 자동 빌드하므로 배포 시 반영
- `docs/security/error-pages.md` 체크리스트 갱신 (프론트 3종 완료 표시)
- 참고: resume/portfolio는 프론트 없음 → 대상 아님

### nginx 보안 강화 + git 관리 전환 (커밋 `db076c1`)
- `infra/nginx/sh-platform.conf`: 실제 서버 설정(`/etc/nginx/sites-available/sh-platform`)을 저장소로 가져와 하드닝 추가
  - `server_tokens off`, `X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy`
  - `error_page 502 503 504 → /502.html` (브랜딩 점검 페이지, `infra/nginx/502.html`)
- 배포 워크플로에 nginx 동기화 단계 추가 (502.html + conf 복사 + `nginx -t` + reload)

### 핵심 함정 발견/해결 (커밋 `215b297`)
- 서버 `sites-enabled/sh-platform` 이 **심링크가 아닌 독립 파일**이라 `sites-available` 복사만으로는 로드 안 됨
- `nginx -T` 로 판명 → `sites-enabled` 에도 복사하도록 워크플로 수정, 수동 즉시 적용 완료
- 상세: `docs/guides/2026-08-01-nginx-hardening.md`

### SSH 접속 불가 (로컬)
- 이 PC에 `~/.ssh/config` 없음, 서버 키 없음 → 제가 서버 직접 조치 불가, 사용자가 서버 터미널에서 실행
- GitHub 시크릿 `OCI_SSH_PW` 값은 API 조회 불가 → 향후 SSH 키 방식(`ssh oci-web`) 세팅 권장

## 5. CSP Report-Only 적용 + 플랫폼 로그아웃 404 수정 — 커밋 5847024, e4b2470

### CSP (커밋 `5847024`)
- nginx에 `Content-Security-Policy-Report-Only` 추가 (차단 없이 위반 기록)
- 정책: `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; font-src 'self' data:; base-uri 'self'; form-action 'self'; frame-ancestors 'none'`
- 3개 SPA 모두 자기 도메인 리소스만 사용 확인 → 위반 없음 (사용자 브라우저 콘솔 확인 완료)
- **남은 작업**: Report-Only → 강제 CSP 전환 + `/grafana/`, Swagger 정상 동작 확인

### 플랫폼 로그아웃 404 (커밋 `e4b2470`)
- 증상: `/platform` 로그아웃 시 404 + 대시보드/로그아웃 동작 안 함
- 원인: `navigate("/")`가 App 재렌더 없이 라우팅되어 `/`가 `*` catch-all(NotFound)에 걸림
- 수정: `window.location.replace("/")` 전체 리로드로 변경
- 상세: `docs/guides/2026-08-01-spa-404-routing.md`

### SPA 404/라우팅 문서
- `docs/guides/2026-08-01-spa-404-routing.md` — 프론트 3종 404/ErrorBoundary 구현 및 로그아웃 버그 정리

## 6. 스크래퍼 검색 조건 개선 — 경력 범위 바 + 복수 지역 (미커밋)

### 배경
- 기존: 경력 = 라디오 6개(버킷 문자열), 지역 = 라디오 7개(단일), 백엔드 `SearchRequest.location` 단일 String
- 사용자 요구: 경력=범위 바, 지역=국토부 기준 주요 도시 기본 선택 + 멀티 셀렉트박스

### API 계약 변경
- `SearchRequest` 확장: `careerMin`/`careerMax`(int, 0~15년) + `locations`(배열) 추가, 레거시 `career`/`location` 하위호환 유지
- 표준 파라미터 직렬화: `careerMin`/`careerMax`는 별도 키, `location`은 콤마 결합(`"서울,경기"`)
- 서버 필터: `matchesCareerRange`(범위 겹침) + `matchesLocationAny`(ANY 포함) — 크롤러가 못 걸렀을 때 최종 안전망
- `SiteSearchMapper.mapValue` 다중 값 지원: `"서울,경기"` → `loc_cd=101000,102000` (매핑 없으면 해당 파라미터 생략)

### 크롤러별 적용 (베스트 에포트)
| 사이트 | 경력 범위 | 복수 지역 |
|--------|-----------|-----------|
| saramin | `exp_cd=2&exp_min&exp_max` | 단일만 `loc_mcd`, 복수 미전송 |
| jobkorea | `careerList=["2"]&careerMin&careerMax` | `locationList` 배열 |
| wanted | `years={min}` (min>0만) | `locations` 콤마 전송 |
| remember | `min_experience={min}` (min>0만) | 단일만 `sido`, 복수 미전송 |

### 프론트
- 신규 `src/components/SearchFilters.tsx`: `REGIONS`(17개 시/도), `DEFAULT_LOCATIONS`(주요 도시 9개 기본), `CareerRangeSlider`(0~15년 듀얼 슬라이더, CSS는 index.css `.range-slider`), `LocationMultiSelect`
- Search.tsx: 전체 범위(0~15)·17개 전체 선택이면 필터 미전송, Schedule로 careerMin/careerMax/locations 전달
- Schedule.tsx: 동일 컴포넌트 재사용, localStorage 레거시 문자열 자동 변환(`legacyCareerRange`)

### 검증
- 백엔드: `./gradlew :modules:scraper:backend:test` 통과 (SearchServiceTest 경력 범위/복수 지역 케이스, SiteSearchMapperTest 복수 지역, CrawlerMappingTest Saramin 복수→빈값 추가)
- 프론트: `npm run build`(tsc + vite) 성공, `oxlint` 경고 없음(기존 FileTree 경고 1건만)
- 설계 문서 갱신: `docs/scraper/search-parameter-standardization.md` §12 추가, §9 상태표 완료 표시

## 7. 스크래퍼 계정 격리(테넌트) — 1단계 구현·배포 — 커밋 7ca8751, eda9457, aedb771

### 설계 (요건 문서 §1.6)
- 계정 식별: JWT `sub`(=auth users.id)를 `crawl_config.account_id`로 저장 (크로스 DB, FK 없음)
- MD 경로: `{root}/{account_id}/{schedule_name}/{YYYY-MM-DD}.md`, root=`/home/ubuntu/data/scraper`
- JWT 공개키 중앙 `.env`(`JWT_PUBLIC_KEY`)로 이전, yml은 `${JWT_PUBLIC_KEY:기존키}` 기본값 유지
- 기존 crawl_config 데이터는 `account_id=1` 귀속, 유니크: 전역 `name` → `(account_id, name)`

### 구현 (커밋 7ca8751)
| 영역 | 변경 |
|------|------|
| common | `JwtTokenValidator`(RS256, `.env` 키), `JwtAuthenticationFilter`(Bearer→SecurityContext), `SecurityUtils.currentAccountId()` + `spring-security-oauth2-jose` 의존성 |
| scraper SecurityConfig | JWT 필터 + permitAll 화이트리스트 + 401/403 JSON 핸들러 |
| scraper API | crawl-config/execution/log/site-config/docs-crawlers 전부 계정 필터(소유권 검증) 적용 |
| scraper model/service | `CrawlConfig.accountId`(기본 1), 저장소 계정 필터 메서드, `localPath` 자동 생성(`/home/ubuntu/data/scraper/{accountId}/{safeName}`) |
| 프론트 | `scraper.ts`/`FileTree`/`Viewer`에 `Authorization: Bearer` 헤더 추가 |
| 설정 | scraper·auth·resume·portfolio yml에 `jwt.public-key` 설정 |

### 배포 (커밋 eda9457, aedb771) — 3번의 배포 실행
- **함정 1**: `deploy-backend.yml`에 DDL 단계가 없어 scraper(`ddl-auto:validate`)가 새 컬럼 없이 기동 실패할 뻔 → 워크플로에 서비스 재시작 전 `mysql < docs/scraper/ddl-account-isolation.sql` 추가 + DDL 멱등화(`IF NOT EXISTS`) — 커밋 eda9457
- **함정 2**: common의 `JwtTokenValidator`(@Component, `@Value("${jwt.public-key}")`) 때문에 resume/portfolio가 프로퍼티 미정의로 **기동 실패** → 두 yml에 `jwt.public-key` 추가 — 커밋 aedb771
- 배포 확인: 진행 중이던 첫 실행은 `gh run cancel`로 취소 후 재배포 (총 2회 성공)

### 검증 (공개 엔드포인트)
- auth/scraper/resume/portfolio `actuator/health` 전부 200
- `/scraper/docs/crawlers`, `/scraper/crawl-config` → **401** + JSON `{"code":"UNAUTHORIZED",...}` (JWT 필터 동작 확인)
- `/scraper/v3/api-docs` 200, swagger-ui 200, SPA index 200

### 남은 작업 (P0, 이후 단계)
- Schedule.tsx localStorage → 서버 API 연동 (per-config cron·siteConfig)
- 뷰어 `/docs/list`·`/docs/jobs` 백엔드 미구현
- 알림 확장(메일/카톡), 신규 공고 dedup lookback 하드코딩 → 설정화


## 7.1 Schedule 서버 연동 (fix: d0a71a5)

- 원인: Schedule.tsx가 localStorage(`schedule_${name}`)에만 저장 → 서버 목록/뷰어(`/docs/crawlers`)에 반영 안 됨 → "저장 안 됨"으로 보임.
- 수정: scraper.ts에 `fetchSites/saveCrawler/updateCrawler/deleteCrawler/saveSiteConfig` + `errorMessage` 헬퍼 추가. Schedule.tsx는 POST/PUT `/crawl-config`로 config 저장 → `fetchSites`로 siteDefinitionId 매핑 → `saveSiteConfig(configId, siteDefId, {paramValues, isEnabled})`로 검색조건(keyword/careerMin/careerMax/locations) 저장. 목록은 GET `/crawl-config` 기반. 저장/실패 시 alert로 오류 표시(기존 "확인도 안 되는" 문제 해소).
- paramValues 규칙(Search.tsx와 동일): careerMin/Max는 isCareerActive 시에만, locations는 전체가 아닐 때만 전송. 비활성 사이트는 기존 paramValues 유지.
- 검증: 로컬 `npm run build`(tsc -b && vite build) 성공, `npm run lint`(oxlint) 기존 FileTree 경고만.
- 배포: push master → GitHub Actions 자동 배포 성공.
- 함정: 배포 직후 scraper 502(Bad Gateway) → auth/resume은 정상, scraper만 다운. 약 수십 초 후 자동 복구됨(서비스 기동 지연/포트 정리 지연). 복구 후 검증:
  - `/scraper/actuator/health` → UP
  - `/scraper/` → 200, 번들 `index-DcRNPsPe.js`에 새 저장 로직 마커 확인(`스케줄이 저장되었습니다`, `site-configs` 등)
  - `/scraper/crawl-config` → 토큰 없이 401 (계정 격리 유지)
- 남은 작업(P1): per-config cron은 아직 미사용(전역 cron으로 실행) — 스케줄 값은 DB 저장됨.
