# 2026-09-02 작업 일지

## 작업 환경
- 날짜: 2026-09-02 (수)
- 브랜치: master

## 오늘 완료된 작업
1. **회사 작업 pull 반영** (12커밋 `7e42d92..a53253e`)
   - `6515184` 블랙리스트 카테고리별 차단 통계(데이터 마이닝) `GET /scraper/company-blacklist/stats` + platform 대시보드 차단 회사 유형/사유 카드 — 우리가 '다음' 후보로 잡았던 작업이 회사에서 이미 완료·배포됨
   - `056420b` 학교/전공 마스터 확장 (ddl-resume-v7)
   - `5832bb2`/`5d3958c` Redis 유령 세션 lazy cleanup(A-1)·기기별 로그아웃(A-2) 서버 검증 완료 + 플랜 016 완료 마킹
   - `88b02d6`~`a53253e` (8커밋) 이력서 서버사이드 PDF(OpenPDF) — 테마별(Classic/Modern/Saramin) 레이아웃 + 파일명(테마+제목) + 페이지 분리(ensureRoom/KeepTogether) + 사진 프레임 통일 + 다중 페이지 회귀 테스트, ContentDispositionSupport(RFC 5987) 등
2. **배포 검증 (서버 SSH 확인)** — 전 항목 정상 반영 확인
   - auth/scraper/resume systemd 全 active
   - `GET /scraper/company-blacklist/stats` → 401(존재, 보호 정상)
   - `PUT /scraper/company-blacklist/1` → 401 (수정 기능 반영, 404/405 아님)
   - `GET /resume/api/v1/view/pdf` → 401 (이력서 PDF 반영)
   - CSP 헤더: `script-src 'self' https://t1.daumcdn.net` — 우리 세션의 CSP 작업 반영 확인
3. **resume 다음 단계 기획 — Phase 9 공유 링크 설계 문서 작성**
   - Phase 매핑 완료: resume Phase 3~8 + PDF(백로그) 전부 구현 확인 → 미구현은 **Phase 9(공유 링크)** 가 유일
   - `docs/plans/024-260902-resume-share-link-design.md` 신규 작성: `resume_share_links` 테이블(문서당 1개), 토큰 UUID v4, 소유자 생성/조회/해제 API + `/share/{token}(/pdf)` 공개 조회(security whitelist + noindex), 기존 `ResumeViewService`/`ResumePdfService` 재사용, 구현 8단계 계획
4. **Phase 9 공유 링크 구현 (설계 024) — 백엔드 + 프론트 완료**
   - DDL v8: `resume_share_links` 테이블 + `deploy-backend.yml`에 v8 라인 추가
   - Entity/Repository: `ResumeShareLinkEntity`(static create + @PrePersist), `ResumeShareLinkRepository`(`findByToken`/`findByDocumentId`/`deleteByDocumentId`)
   - 도메인: `ResumeShareService(+Impl)` — `createShareLink`(재발급)/`getShareLink`/`revokeShareLink`/`resolve`/`getPublicView` + 소유자 검증(`findByIdAndUserId` → NOT_FOUND), 토큰 UUID v4(32자), 만료 시 404
   - 공개 뷰 조립: 기존 `ResumeViewService.getMyResumeView` + `ResumeDocumentService.getDocuments` 조합 `getPublicView`
   - 관리 API: `DocumentsController`에 `POST/GET/DELETE /api/v1/documents/{id}/share` + `ShareLinkCreateRequest`/`ShareLinkResponse`/`ShareViewResponse` DTO
   - 공개: `ShareController` `GET /api/.../share/{token}`(noindex 헤더) + `/share/{token}/pdf`(Content-Disposition + noindex) + `SecurityConfig` whitelist `/share/**`
   - 테스트: `ResumeShareServiceImplTest` 11건 (생성/재발급/조회/해제/만료/타소유자 NOT_FOUND/공개 조회) — 전체 resume 테스트 통과
    - 프론트: `ShareViewPage`(AppShell 없이 public 뷰 + PDF), `ResumesPage` 공유 토글·링크 복사/해제, `App.tsx` 라우트 `#/s/{token}` 추가, `client.ts` 공개 API 헬퍼(`apiGetShare`/`apiDownloadShare`), `types/document.ts` `ShareLink` 추가 — `tsc -b` 통과 (로컬 vite rocldown 네이티브 바인딩 이슈로 산출물 빌드는 CI에서 수행)
5. **배포 후 실검증에서 공개 뷰 500 발견 → 수정 + 재검증 완료**
   - 현상: `/share/{token}` 공개 조회 시 `UnexpectedRollbackException`(Transaction silently rolled back because marked rollback-only) → 500
   - 근본 원인: `getPublicView`/`resolve`에 `@Transactional(readOnly=true)`가 적용되어 있고, 그 안에서 `ResumeViewService.getMyResumeView`/`ResumeDocumentService.getDocuments`(내부 서비스가 NOT_FOUND 등 예외를 던짐)가 호출되면 공유 트랜잭션이 rollback-only로 마킹되어 커밋 실패
   - 해결: `ResumeViewServiceImpl`의 기존 원칙(트랜잭션 미적용 + 내부 예외 흡수)과 동일하게 `getPublicView`/`resolve`에서 `@Transactional` 제거 → 커밋 `95ae899` → CI 재배포
   - 재검증 (서버 실서버 플로우): 공개 뷰 200 / 공개 PDF 200(application/pdf) / 소유자 GET 200 / revoke 200 / revoke 후 공개 뷰 404 / 과거 만료일 생성 거부(`@Future` INVALID_INPUT "must be a future date") — 만료 시나리오는 생성 단계에서 선차단되며, 시간 경과 만료 토큰은 `resolve`의 `isAfter(now)` 필터가 404 처리(로그·테스트로 확인). bad token 404는 revoke 후 404와 동일한 `resolve→Optional.empty→NOT_FOUND` 경로.

## 변경된 파일 목록
- `docs/daily/2026-09-02-work-log.md`: 신규 — 본 일지 (Phase 9 구현 완료 반영)
- `AGENTS.md`: 진행 중 작업 배너 갱신 (배포 검증 완료 상태)
- `docs/plans/024-260902-resume-share-link-design.md`: 신규 — Phase 9 공유 링크 설계 (구현 상태 반영)
- `docs/resume/ddl-resume-v8.sql`: 신규 — `resume_share_links` 테이블
- `.github/workflows/deploy-backend.yml`: resume DDL v8 라인 추가
- `modules/resume/backend/.../infrastructure/entity/ResumeShareLinkEntity.java`: 신규
- `modules/resume/backend/.../infrastructure/repository/ResumeShareLinkRepository.java`: 신규
- `modules/resume/backend/.../domain/ResumeShareService.java` / `ResumeShareServiceImpl.java`: 신규
- `modules/resume/backend/.../api/DocumentsController.java`: 공유 관리 3종 엔드포인트 추가
- `modules/resume/backend/.../api/ShareController.java`: 신규 — 공개 조회/PDF
- `modules/resume/backend/.../api/dto/ShareLinkCreateRequest.java` / `ShareLinkResponse.java` / `ShareViewResponse.java`: 신규
- `modules/resume/backend/.../config/SecurityConfig.java`: `/share/**` whitelist 추가
- `modules/resume/backend/src/test/.../ResumeShareServiceImplTest.java`: 신규 — 11건
- `modules/resume/frontend/src/pages/ShareViewPage.tsx`: 신규 — 공개 뷰
- `modules/resume/frontend/src/pages/ResumesPage.tsx`: 공유 토글·링크 복사/해제
- `modules/resume/frontend/src/App.tsx`: `#/s/{token}` 라우트
- `modules/resume/frontend/src/api/client.ts`: `apiGetShare`/`apiDownloadShare`
- `modules/resume/frontend/src/types/document.ts`: `ShareLink` 타입

## 이슈 및 해결
- (pull) — pull은 fast-forward로 충돌 없음, 배포 검증에서 특이사항 없음
- (프론트 빌드) — 로컬 `npm run build`(vite)가 `@rolldown/binding-darwin-arm64` 네이티브 바인딩 부재로 실패. 원인은 로컬 환경 제약(가상머신 아키텍처)이며, `tsc -b` 타입 체크는 통과. 프론트 dist는 CI(`deploy-backend.yml`) ubuntu에서 정상 빌드되므로 기능 영향 없음.
- (공개 뷰 500) — `ResumeShareServiceImpl.getPublicView`/`resolve`의 `@Transactional(readOnly=true)`가 내부 뷰 조립 서비스 예외로 rollback-only 마킹 → `UnexpectedRollbackException`. `ResumeViewServiceImpl` 원칙(트랜잭션 미적용)과 동일하게 해당 메서드에서 `@Transactional` 제거로 해결. → **학습**: 내부 서비스들이 예외를 던질 수 있는 public 조회(void 저장이 아닌) 메서드에는 읽기 전용 공유 트랜잭션을 걸지 말 것.

## 커밋 내역
| 시간 | 커밋 | 내용 |
|------|------|------|
| - | 64ec5c7 | docs: 회사 pull 반영 배포 검증 + 09-02 작업일지 (2차 커밋 — Phase 9 설계 미포함, 후속 커밋 대기) |
| - | 8d6561f | feat: resume Phase 9 공유 링크 구현 (DDL v8 + ShareService + ShareController + 프론트) |
| - | 95ae899 | fix: 공유 뷰 getPublicView/resolve 트랜잭션 제거로 UnexpectedRollbackException 500 해결 |

## 향후 작업
1. **Phase 9 공유 링크 마무리** — 구현·배포·검증 완료. 실사용자 눈 검증 + `/resume/#/s/{token}` 공개 URL 최종 확인
2. **이력서 PDF 개선분 사용자 최종 검증** (08-31 향후 #1) — 배포 확인 완료, 실사용자 눈 검증 대기
3. **로드맵 백로그**: 공고-이력서 키워드 매칭 점수, AI 자소 첨삭, 이메일 알림
