spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3307/resume_platform
    driver-class-name: org.mariadb.jdbc.Driver
    username: sh_local
    password: sh_local_pass
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    com.shplatform.resume: DEBUG